明白你的需求。你要的是**上机考试速查手册**，不是教材式讲解，所以我会按照：

> **① 名称（函数/知识点）  
> ② 用法（参数怎么写 + 示例代码）  
> ③ 作用（考试时知道什么时候用）**

来整理。

由于内容非常庞大（尤其 `vector` + `string` + `algorithm` + `map/set/queue/stack`），一次回复放不下完整版本。我会分多次连续发送。

整体目录如下：

# C++ STL 上机考试速查大全

## 第一部分：vector（极详细）

- vector定义
- 初始化方式
- 遍历方式
- 所有成员函数
  - size()
  - capacity()
  - empty()
  - resize()
  - reserve()
  - push_back()
  - emplace_back()
  - pop_back()
  - insert()
  - erase()
  - clear()
  - front()
  - back()
  - at()
  - swap()
  - assign()
  - data()
  - begin()
  - end()
  - rbegin()
  - rend()

## 第二部分：string（极详细）

- 创建
- 输入输出
- 拼接
- 查找
- 截取
- 替换
- 删除
- 插入
- 字符转换
- 所有常用函数

## 第三部分：algorithm

- sort
- reverse
- max/min
- swap
- find
- count
- accumulate
- fill
- replace
- unique
- lower_bound
- upper_bound
- binary_search
- next_permutation
- prev_permutation
- rotate
- partition

## 第四部分：deque

## 第五部分：stack

## 第六部分：queue

## 第七部分：priority_queue

## 第八部分：set / multiset

## 第九部分：map / unordered_map

## 第十部分：pair

## 第十一部分：迭代器 iterator

## 第十二部分：常用万能模板

---

下面开始。

# 第一章 vector（重点）

头文件：

```cpp
#include <vector>
using namespace std;
```

---

# 1. vector定义

## 名称

```cpp
vector
```

## 用法

```cpp
vector<int> v;
```

定义一个存放 int 的动态数组。

其他：

```cpp
vector<double> v1;

vector<string> v2;

vector<char> v3;
```

二维：

```cpp
vector<vector<int>> matrix;
```

相当于二维数组。

## 作用

vector 是 STL 中最常用的动态数组。

和普通数组区别：

普通数组：

```cpp
int a[100];
```

大小固定。

vector：

```cpp
vector<int> v;
```

可以自动扩容。

---

# 2. vector初始化

## 名称

初始化

## 用法

### 空vector

```cpp
vector<int> v;
```

创建：

```
{}
```

---

### 指定数量

```cpp
vector<int> v(5);
```

结果：

```
0 0 0 0 0
```

---

### 指定数量和值

```cpp
vector<int> v(5,10);
```

结果：

```
10 10 10 10 10
```

---

### 数组初始化

```cpp
int a[]={1,2,3};

vector<int> v(a,a+3);
```

结果：

```
1 2 3
```

---

### 直接赋值

```cpp
vector<int> v={1,2,3,4};
```

结果：

```
1 2 3 4
```

## 作用

创建vector。

---

# 3. push_back()

## 名称

```cpp
push_back()
```

## 用法

格式：

```cpp
vector名.push_back(元素);
```

例如：

```cpp
vector<int> v;

v.push_back(10);

v.push_back(20);
```

结果：

```
10 20
```

## 作用

在vector末尾添加元素。

考试最常用。

---

# 4. pop_back()

## 名称

```cpp
pop_back()
```

## 用法

```cpp
v.pop_back();
```

例如：

```cpp
vector<int> v={1,2,3};

v.pop_back();
```

结果：

```
1 2
```

## 作用

删除最后一个元素。

注意：

不能指定删除哪个。

只能删除尾部。

---

# 5. size()

## 名称

```cpp
size()
```

## 用法

```cpp
v.size();
```

例如：

```cpp
vector<int> v={1,2,3};

cout<<v.size();
```

输出：

```
3
```

## 作用

返回vector当前元素数量。

注意：

不是容量。

例如：

```cpp
vector<int> v(100);
```

size:

```
100
```

---

# 6. capacity()

## 名称

```cpp
capacity()
```

## 用法

```cpp
v.capacity();
```

例如：

```cpp
vector<int> v;

v.push_back(1);

cout<<v.capacity();
```

可能输出：

```
1
```

或者：

```
2
```

## 作用

查看vector当前申请的内存空间。

区别：

| 函数       | 作用             |
| ---------- | ---------------- |
| size()     | 已有多少元素     |
| capacity() | 最多能放多少元素 |

考试一般用size。

---

# 7. empty()

## 名称

```cpp
empty()
```

## 用法

```cpp
v.empty();
```

例如：

```cpp
vector<int> v;

if(v.empty())
{
    cout<<"空";
}
```

返回：

```
true
```

## 作用

判断vector是否为空。

等价：

```cpp
v.size()==0
```

---

# 8. clear()

## 名称

```cpp
clear()
```

## 用法

```cpp
v.clear();
```

例如：

```cpp
vector<int> v={1,2,3};

v.clear();
```

结果：

```
{}
```

## 作用

删除所有元素。

注意：

不会释放空间。

例如：

```cpp
v.clear();

v.capacity();
```

容量可能仍然存在。

---

# 9. front()

## 名称

```cpp
front()
```

## 用法

```cpp
v.front();
```

例如：

```cpp
vector<int> v={10,20,30};

cout<<v.front();
```

输出：

```
10
```

## 作用

获取第一个元素。

等价：

```cpp
v[0]
```

---

# 10. back()

## 名称

```cpp
back()
```

## 用法

```cpp
v.back();
```

例如：

```cpp
vector<int> v={10,20,30};

cout<<v.back();
```

输出：

```
30
```

## 作用

获取最后一个元素。

等价：

```cpp
v[v.size()-1]
```

---

# 11. 下标访问 []

## 名称

```cpp
operator[]
```

## 用法

```cpp
v[index];
```

例如：

```cpp
vector<int> v={5,6,7};

cout<<v[1];
```

输出：

```
6
```

## 作用

访问指定位置元素。

注意：

不检查越界。

---

# 12. at()

## 名称

```cpp
at()
```

## 用法

```cpp
v.at(index);
```

例如：

```cpp
vector<int> v={1,2,3};

cout<<v.at(1);
```

输出：

```
2
```

## 作用

访问元素。

区别：

```cpp
v[10];
```

越界可能崩溃。

```cpp
v.at(10);
```

会主动报错。

---

# 13. begin()

## 名称

```cpp
begin()
```

## 用法

```cpp
v.begin();
```

例如：

```cpp
vector<int> v={1,2,3};

cout<<*v.begin();
```

输出：

```
1
```

## 作用

返回第一个元素的迭代器。

常用于：

```cpp
sort(v.begin(),v.end());
```

---

# 14. end()

## 名称

```cpp
end()
```

## 用法

```cpp
v.end();
```

例如：

```cpp
sort(v.begin(),v.end());
```

## 作用

返回最后一个元素后面的位置。

注意：

不是最后一个元素。

最后元素：

```cpp
*(v.end()-1)
```

---

# 15. rbegin()

## 名称

```cpp
rbegin()
```

## 用法

```cpp
v.rbegin();
```

例如：

```cpp
vector<int> v={1,2,3};

cout<<*v.rbegin();
```

输出：

```
3
```

## 作用

反向迭代器开始位置。

---

# 16. rend()

## 名称

```cpp
rend()
```

## 用法

```cpp
v.rend();
```

例如：

```cpp
for(auto i=v.rbegin();i!=v.rend();i++)
{
    cout<<*i;
}
```

输出：

```
321
```

## 作用

反向遍历结束位置。

---

# 17. insert()

## 名称

```cpp
insert()
```

## 用法

格式：

```cpp
v.insert(位置,元素);
```

例如：

```cpp
vector<int> v={1,3};

v.insert(v.begin()+1,2);
```

结果：

```
1 2 3
```

---

插入多个：

```cpp
v.insert(v.begin(),3,10);
```

结果：

```
10 10 10 ...
```

## 作用

在指定位置插入元素。

---

# 18. erase()

## 名称

```cpp
erase()
```

## 用法

删除一个：

```cpp
v.erase(v.begin()+2);
```

例如：

```cpp
vector<int> v={1,2,3};

v.erase(v.begin()+1);
```

结果：

```
1 3
```

删除区间：

```cpp
v.erase(v.begin(),v.begin()+2);
```

删除：

```
前两个元素
```

## 作用

删除指定位置或者范围元素。

---

# 19. resize()

## 名称

```cpp
resize()
```

## 用法

改变大小：

```cpp
v.resize(10);
```

不足补0。

例如：

```cpp
vector<int> v={1,2};

v.resize(5);
```

结果：

```
1 2 0 0 0
```

指定值：

```cpp
v.resize(5,100);
```

结果：

```
1 2 100 100 100
```

## 作用

改变vector元素数量。

---

# 20. reserve()

## 名称

```cpp
reserve()
```

## 用法

```cpp
v.reserve(100);
```

## 作用

提前申请空间。

区别：

resize：

改变元素数量。

reserve：

只改变容量。

例如：

```cpp
vector<int> v;

v.reserve(100);

cout<<v.size();
```

输出：

```
0
```

---

# 21. swap()

## 名称

```cpp
swap()
```

## 用法

两个vector交换：

```cpp
v1.swap(v2);
```

例如：

```cpp
vector<int> a={1,2};

vector<int> b={3,4};

a.swap(b);
```

结果：

a:

```
3 4
```

b:

```
1 2
```

## 作用

交换两个vector内容。

---

# 22. assign()

## 名称

```cpp
assign()
```

## 用法

数量和值：

```cpp
v.assign(5,10);
```

结果：

```
10 10 10 10 10
```

区间：

```cpp
v.assign(v2.begin(),v2.end());
```

## 作用

重新给vector赋值。

---

# 23. data()

## 名称

```cpp
data()
```

## 用法

```cpp
int *p=v.data();
```

例如：

```cpp
vector<int> v={1,2,3};

int *p=v.data();

cout<<p[0];
```

输出：

```
1
```

## 作用

返回底层数组指针。

用于和C语言数组交互。

---

# vector遍历（考试必背）

## 方法1：下标

```cpp
for(int i=0;i<v.size();i++)
{
    cout<<v[i];
}
```

---

## 方法2：范围for

```cpp
for(auto x:v)
{
    cout<<x;
}
```

---

## 方法3：迭代器

```cpp
for(auto it=v.begin();it!=v.end();it++)
{
    cout<<*it;
}
```

---

## 方法4：反向

```cpp
for(auto it=v.rbegin();it!=v.rend();it++)
{
    cout<<*it;
}
```

---

继续：

# 第二章 string（极重要）

头文件：

```
#include <string>
using namespace std;
```

string 是 STL 中处理字符串的容器。

相比 C 语言：

```
char s[100];
```

string：

```
string s;
```

不用考虑长度，不需要手动管理 `'\0'`。

---

# 1. string定义

## 名称

```
string
```

## 用法

普通定义：

```
string s;
```

初始化：

```
string s="hello";
```

多个：

```
string a="abc";
string b="def";
```

## 作用

创建字符串对象。

---

# 2. string输入

## 名称

`cin`

## 用法

### 输入一个单词

```
string s;

cin>>s;
```

输入：

```
hello
```

结果：

```
s="hello"
```

注意：

遇到空格停止。

---

### 输入整行

## 名称

```
getline()
```

## 用法

```
getline(cin,s);
```

例如：

```
string s;

getline(cin,s);

cout<<s;
```

输入：

```
hello world
```

输出：

```
hello world
```

## 作用

读取包含空格的一整行字符串。

考试非常常用。

---

# 3. size()

## 名称

```
size()
```

## 用法

```
s.size();
```

例如：

```
string s="hello";

cout<<s.size();
```

输出：

```
5
```

## 作用

获取字符串长度。

---

# 4. length()

## 名称

```
length()
```

## 用法

```
s.length();
```

例如：

```
string s="abc";

cout<<s.length();
```

输出：

```
3
```

## 作用

获取字符串长度。

注意：

和 size() 完全一样。

---

# 5. empty()

## 名称

```
empty()
```

## 用法

```
s.empty();
```

例如：

```
string s="";

if(s.empty())
{
    cout<<"空";
}
```

## 作用

判断字符串是否为空

---

# 6. 字符访问 `[]`

## 名称

```cpp
operator[]
```

## 用法

格式：

```cpp
s[index];
```

例如：

```cpp
string s="hello";

cout<<s[0];
```

输出：

```
h
```

修改：

```cpp
s[0]='H';
```

结果：

```
Hello
```

## 作用

访问或者修改指定位置字符。

注意：

下标从 **0开始**。

---

# 7. at()

## 名称

```cpp
at()
```

## 用法

```cpp
s.at(index);
```

例如：

```cpp
string s="abc";

cout<<s.at(1);
```

输出：

```
b
```

修改：

```cpp
s.at(1)='B';
```

结果：

```
aBc
```

## 作用

访问指定位置字符。

区别：

```cpp
s[100];
```

越界不检查。

```cpp
s.at(100);
```

会抛异常。

---

# 8. front()

## 名称

```cpp
front()
```

## 用法

```cpp
s.front();
```

例如：

```cpp
string s="hello";

cout<<s.front();
```

输出：

```
h
```

## 作用

获取第一个字符。

等价：

```cpp
s[0]
```

---

# 9. back()

## 名称

```cpp
back()
```

## 用法

```cpp
s.back();
```

例如：

```cpp
string s="hello";

cout<<s.back();
```

输出：

```
o
```

## 作用

获取最后一个字符。

等价：

```cpp
s[s.size()-1]
```

---

# 10. push_back()

## 名称

```cpp
push_back()
```

## 用法

格式：

```cpp
s.push_back(字符);
```

例如：

```cpp
string s="abc";

s.push_back('d');
```

结果：

```
abcd
```

注意：

只能添加一个字符。

错误：

```cpp
s.push_back("hello");
```

正确：

```cpp
s+="hello";
```

## 作用

在字符串末尾添加字符。

---

# 11. pop_back()

## 名称

```cpp
pop_back()
```

## 用法

```cpp
s.pop_back();
```

例如：

```cpp
string s="hello";

s.pop_back();
```

结果：

```
hell
```

## 作用

删除最后一个字符。

---

# 12. append()

## 名称

```cpp
append()
```

## 用法

添加字符串：

```cpp
s.append("abc");
```

例如：

```cpp
string s="hello";

s.append(" world");
```

结果：

```
hello world
```

也可以：

```cpp
s.append(t);
```

其中：

```cpp
string t="abc";
```

## 作用

在末尾追加字符串。

等价：

```cpp
s+="abc";
```

---

# 13. 字符串连接 `+`

## 名称

字符串拼接

## 用法

```cpp
s1+s2;
```

例如：

```cpp
string a="hello";

string b="world";

string c=a+b;
```

结果：

```
helloworld
```

添加字符：

```cpp
s+='!';
```

结果：

```
hello!
```

## 作用

连接两个字符串。

---

# 14. insert()

## 名称

```cpp
insert()
```

## 用法

格式：

```cpp
s.insert(位置,字符串);
```

例如：

```cpp
string s="abcd";

s.insert(2,"XX");
```

过程：

```
ab + XX + cd
```

结果：

```
abXXcd
```

---

插入一个字符：

```cpp
s.insert(0,1,'A');
```

结果：

```
Aabcd
```

## 作用

在指定位置插入内容。

---

# 15. erase()

## 名称

```cpp
erase()
```

## 用法1：删除指定位置

格式：

```cpp
s.erase(pos);
```

例如：

```cpp
string s="abcdef";

s.erase(2);
```

结果：

```
ab
```

从位置2开始全部删除。

---

## 用法2：删除指定长度

格式：

```cpp
s.erase(pos,len);
```

例如：

```cpp
string s="abcdef";

s.erase(2,3);
```

删除：

```
cde
```

结果：

```
abf
```

## 作用

删除字符串中的字符。

---

# 16. clear()

## 名称

```cpp
clear()
```

## 用法

```cpp
s.clear();
```

例如：

```cpp
string s="hello";

s.clear();
```

结果：

```
""
```

## 作用

清空字符串。

---

# 17. substr()

## 名称

```cpp
substr()
```

## 用法

格式：

```cpp
s.substr(开始位置,长度);
```

例如：

```cpp
string s="abcdef";

string t=s.substr(2,3);
```

从：

```
c开始
取3个
```

结果：

```
cde
```

---

省略长度：

```cpp
s.substr(2);
```

结果：

```
cdef
```

## 作用

截取字符串。

考试非常常用。

---

# 18. find()

## 名称

```cpp
find()
```

## 用法

格式：

```cpp
s.find(查找内容);
```

例如：

```cpp
string s="hello";

cout<<s.find("ll");
```

输出：

```
2
```

因为：

```
h e l l o
0 1 2 3 4
```

---

查找字符：

```cpp
s.find('o');
```

输出：

```
4
```

---

不存在：

```cpp
s.find("xx");
```

返回：

```cpp
string::npos
```

例如：

```cpp
if(s.find("abc")!=string::npos)
{
    cout<<"存在";
}
```

## 作用

查找子串或者字符第一次出现的位置。

---

# 19. rfind()

## 名称

```cpp
rfind()
```

## 用法

```cpp
s.rfind("abc");
```

例如：

```cpp
string s="abcabc";

cout<<s.rfind("abc");
```

输出：

```
3
```

## 作用

从后往前查找。

返回最后一次出现位置。

---

# 20. replace()

## 名称

```cpp
replace()
```

## 用法

格式：

```cpp
s.replace(位置,长度,替换内容);
```

例如：

```cpp
string s="abcdef";

s.replace(2,3,"XYZ");
```

原：

```
ab cde f
```

替换：

```
ab XYZ f
```

结果：

```
abXYZf
```

## 作用

替换字符串中的部分内容。

---

# 21. compare()

## 名称

```cpp
compare()
```

## 用法

```cpp
s1.compare(s2);
```

例如：

```cpp
string a="abc";

string b="abc";

cout<<a.compare(b);
```

输出：

```
0
```

结果：

| 返回值 | 意义   |
| ------ | ------ |
| 0      | 相等   |
| <0     | a小于b |
| >0     | a大于b |

## 作用

比较字符串。

---

# 22. begin()

## 名称

```cpp
begin()
```

## 用法

```cpp
s.begin();
```

例如：

```cpp
cout<<*s.begin();
```

输出：

```
第一个字符
```

## 作用

返回字符串第一个字符迭代器。

常用于：

```cpp
sort(s.begin(),s.end());
```

---

# 23. end()

## 名称

```cpp
end()
```

## 用法

```cpp
s.end();
```

例如：

```cpp
sort(s.begin(),s.end());
```

## 作用

返回最后一个字符后的位置。

---

# 24. rbegin()

## 名称

```cpp
rbegin()
```

## 用法

```cpp
s.rbegin();
```

例如：

```cpp
cout<<*s.rbegin();
```

输出：

```
最后一个字符
```

## 作用

反向遍历开始。

---

# 25. rend()

## 名称

```cpp
rend()
```

## 用法

```cpp
s.rend();
```

例如：

```cpp
for(auto it=s.rbegin();it!=s.rend();it++)
{
    cout<<*it;
}
```

字符串：

```
abc
```

输出：

```
cba
```

## 作用

反向遍历结束位置。

---

# 26. stoi()

## 名称

```cpp
stoi()
```

## 用法

```cpp
stoi(字符串);
```

例如：

```cpp
string s="123";

int x=stoi(s);
```

结果：

```
x=123
```

## 作用

字符串转整数。

---

# 27. stol()

## 名称

```cpp
stol()
```

## 用法

```cpp
long long x=stoll(s);
```

例如：

```cpp
string s="123456789";

long long x=stoll(s);
```

## 作用

字符串转 long long。

---

# 28. stod()

## 名称

```cpp
stod()
```

## 用法

```cpp
double x=stod(s);
```

例如：

```cpp
string s="3.14";

double x=stod(s);
```

## 作用

字符串转 double。

---

# 29. to_string()

## 名称

```cpp
to_string()
```

## 用法

```cpp
to_string(数字);
```

例如：

```cpp
int x=123;

string s=to_string(x);
```

结果：

```
"123"
```

## 作用

数字转字符串。

---

# 30. sort字符串

## 名称

```cpp
sort()
```

## 用法

```cpp
sort(s.begin(),s.end());
```

例如：

```cpp
string s="dcba";

sort(s.begin(),s.end());
```

结果：

```
abcd
```

## 作用

按照ASCII排序字符。

---

# 31. reverse字符串

## 名称

```cpp
reverse()
```

## 用法

```cpp
reverse(s.begin(),s.end());
```

例如：

```cpp
string s="hello";

reverse(s.begin(),s.end());
```

结果：

```
olleh
```

## 作用

反转字符串。

---

# string考试高频组合

## 1. 判断回文字符串

```cpp
string s;

string t=s;

reverse(t.begin(),t.end());

if(s==t)
    cout<<"yes";
```

作用：

判断正反是否一样。

---

## 2. 统计字符数量

```cpp
int cnt[26]={0};

for(char c:s)
{
    cnt[c-'a']++;
}
```

作用：

统计每个字母出现次数。

---

## 3. 删除空格

```cpp
s.erase(remove(s.begin(),s.end(),' '),s.end());
```

作用：

删除所有空格。

（这里涉及 algorithm 的 remove，后面详细讲）

---

# 第三章 algorithm（STL算法库，考试核心）

头文件：

```cpp
#include <algorithm>
```

部分数学算法需要：

```cpp
#include <numeric>
```

---

# 1. sort()

## 名称

```cpp
sort()
```

---

## 用法

基本格式：

```cpp
sort(开始位置,结束位置);
```

例如 vector：

```cpp
vector<int> v={3,1,5,2};

sort(v.begin(),v.end());
```

结果：

```
1 2 3 5
```

---

数组：

```cpp
int a[]={3,1,5,2};

sort(a,a+4);
```

结果：

```
1 2 3 5
```

---

降序：

格式：

```cpp
sort(begin,end,greater<类型>());
```

例如：

```cpp
sort(v.begin(),v.end(),greater<int>());
```

结果：

```
5 3 2 1
```

---

自定义排序：

格式：

```cpp
sort(begin,end,比较函数);
```

例如：

```cpp
bool cmp(int a,int b)
{
    return a>b;
}

sort(v.begin(),v.end(),cmp);
```

---

## 作用

排序。

考试最常用函数之一。

默认：

```
升序
```

---

# 2. stable_sort()

## 名称

```cpp
stable_sort()
```

---

## 用法

和sort一样：

```cpp
stable_sort(v.begin(),v.end());
```

---

## 作用

稳定排序。

区别：

sort：

相同元素顺序可能改变。

stable_sort：

保证相同元素原来的顺序。

考试：

普通排序直接sort。

---

# 3. reverse()

## 名称

```cpp
reverse()
```

---

## 用法

格式：

```cpp
reverse(开始,结束);
```

例如：

```cpp
vector<int> v={1,2,3,4};

reverse(v.begin(),v.end());
```

结果：

```
4 3 2 1
```

字符串：

```cpp
string s="hello";

reverse(s.begin(),s.end());
```

结果：

```
olleh
```

---

## 作用

反转顺序。

---

# 4. rotate()

## 名称

```cpp
rotate()
```

---

## 用法

格式：

```cpp
rotate(first,middle,last);
```

例如：

```cpp
vector<int> v={1,2,3,4,5};

rotate(v.begin(),v.begin()+2,v.end());
```

过程：

以第3个元素为开头：

```
3 4 5 1 2
```

---

## 作用

旋转数组。

例如：

左旋：

```
1 2 3 4 5

左移2位

3 4 5 1 2
```

---

# 5. swap()

## 名称

```cpp
swap()
```

---

## 用法

```cpp
swap(a,b);
```

例如：

```cpp
int a=10;
int b=20;

swap(a,b);
```

结果：

```
a=20
b=10
```

---

字符串：

```cpp
swap(s1,s2);
```

---

## 作用

交换两个变量。

---

# 6. max()

## 名称

```cpp
max()
```

---

## 用法

两个数：

```cpp
max(a,b);
```

例如：

```cpp
cout<<max(3,5);
```

输出：

```
5
```

---

多个：

```cpp
max({1,5,3});
```

输出：

```
5
```

---

## 作用

返回最大值。

---

# 7. min()

## 名称

```cpp
min()
```

---

## 用法

```cpp
min(a,b);
```

例如：

```cpp
cout<<min(3,5);
```

输出：

```
3
```

---

多个：

```cpp
min({3,1,5});
```

输出：

```
1
```

---

## 作用

返回最小值。

---

# 8. max_element()

## 名称

```cpp
max_element()
```

---

## 用法

格式：

```cpp
max_element(begin,end);
```

注意：

返回的是迭代器。

例如：

```cpp
vector<int> v={3,7,2};

auto it=max_element(v.begin(),v.end());

cout<<*it;
```

输出：

```
7
```

---

## 作用

找容器最大元素位置。

如果想得到下标：

```cpp
int pos=max_element(v.begin(),v.end())-v.begin();
```

---

# 9. min_element()

## 名称

```cpp
min_element()
```

---

## 用法

```cpp
auto it=min_element(v.begin(),v.end());
```

例如：

```cpp
cout<<*min_element(v.begin(),v.end());
```

输出：

```
2
```

---

## 作用

找最小元素。

---

# 10. find()

## 名称

```cpp
find()
```

---

## 用法

格式：

```cpp
find(begin,end,value);
```

例如：

```cpp
vector<int> v={1,2,3,4};

auto it=find(v.begin(),v.end(),3);
```

找到：

```
3
```

输出：

```cpp
cout<<*it;
```

---

不存在：

```cpp
if(it==v.end())
{
    cout<<"不存在";
}
```

---

## 作用

查找元素。

注意：

返回迭代器。

---

# 11. count()

## 名称

```cpp
count()
```

---

## 用法

格式：

```cpp
count(begin,end,value);
```

例如：

```cpp
vector<int> v={1,2,2,3};

int x=count(v.begin(),v.end(),2);
```

结果：

```
2
```

---

字符串：

```cpp
string s="hello";

cout<<count(s.begin(),s.end(),'l');
```

输出：

```
2
```

---

## 作用

统计元素出现次数。

---

# 12. accumulate()

头文件：

```cpp
#include <numeric>
```

## 名称

```cpp
accumulate()
```

---

## 用法

格式：

```cpp
accumulate(begin,end,初始值);
```

三个参数：

1. 开始位置
2. 结束位置
3. 初始值

例如：

```cpp
vector<int> v={1,2,3,4};

int sum=accumulate(v.begin(),v.end(),0);
```

计算：

```
0+1+2+3+4
```

结果：

```
10
```

---

注意：

第三个参数类型决定返回类型。

例如：

```cpp
vector<double> v={1.2,2.3};

double sum=accumulate(v.begin(),v.end(),0.0);
```

必须：

```
0.0
```

不能：

```
0
```

否则可能整数计算。

---

## 作用

求和。

---

# 13. fill()

## 名称

```cpp
fill()
```

---

## 用法

格式：

```cpp
fill(begin,end,value);
```

例如：

数组：

```cpp
int a[5];

fill(a,a+5,10);
```

结果：

```
10 10 10 10 10
```

vector：

```cpp
fill(v.begin(),v.end(),0);
```

---

## 作用

全部赋值。

---

# 14. replace()

## 名称

```cpp
replace()
```

---

## 用法

格式：

```cpp
replace(begin,end,旧值,新值);
```

例如：

```cpp
vector<int> v={1,2,2,3};

replace(v.begin(),v.end(),2,99);
```

结果：

```
1 99 99 3
```

---

字符串：

```cpp
string s="hello";

replace(s.begin(),s.end(),'l','x');
```

结果：

```
hexxo
```

---

## 作用

替换所有指定元素。

---

# 15. unique()

## 名称

```cpp
unique()
```

---

## 用法

常和sort一起：

```cpp
sort(v.begin(),v.end());

v.erase(unique(v.begin(),v.end()),v.end());
```

例如：

原：

```
1 2 2 3 3 3
```

unique：

```
1 2 3 ? ? ?
```

erase删除后：

```
1 2 3
```

---

## 作用

去除连续重复元素。

注意：

单独unique不会真正删除。

---

# 16. lower_bound()

## 名称

```cpp
lower_bound()
```

---

## 用法

要求：

数组必须有序。

格式：

```cpp
lower_bound(begin,end,value);
```

例如：

```cpp
vector<int> v={1,3,5,7};

auto it=lower_bound(v.begin(),v.end(),5);
```

结果：

```
5的位置
```

下标：

```cpp
int pos=it-v.begin();
```

输出：

```
2
```

---

## 作用

寻找第一个：

```
>= value
```

的位置。

例如：

数组：

```
1 3 5 7
```

找4：

返回：

```
5
```

---

# 17. upper_bound()

## 名称

```cpp
upper_bound()
```

---

## 用法

```cpp
upper_bound(v.begin(),v.end(),value);
```

例如：

```cpp
vector<int> v={1,3,5,7};

auto it=upper_bound(v.begin(),v.end(),5);
```

返回：

```
7
```

---

## 作用

寻找第一个：

```
> value
```

的位置。

---

# 18. binary_search()

## 名称

```cpp
binary_search()
```

---

## 用法

要求：

必须排序。

格式：

```cpp
binary_search(begin,end,value);
```

例如：

```cpp
vector<int> v={1,3,5,7};

bool flag=binary_search(v.begin(),v.end(),5);
```

结果：

```
true
```

---

## 作用

二分查找是否存在。

返回：

```
true/false
```

---

# 19. next_permutation()

## 名称

```cpp
next_permutation()
```

---

## 用法

```cpp
next_permutation(begin,end);
```

例如：

```cpp
string s="abc";

next_permutation(s.begin(),s.end());
```

结果：

```
acb
```

连续：

```cpp
while(next_permutation(s.begin(),s.end()))
{
    cout<<s;
}
```

输出所有排列。

---

## 作用

生成下一个字典序排列。

---

# 20. prev_permutation()

## 名称

```cpp
prev_permutation()
```

---

## 用法

```cpp
prev_permutation(s.begin(),s.end());
```

例如：

```
cba
```

变：

```
cab
```

---

## 作用

生成上一个排列。

---

# algorithm 高频考试组合

## 1. vector排序去重

```cpp
sort(v.begin(),v.end());

v.erase(unique(v.begin(),v.end()),v.end());
```

作用：

去除重复元素。

---

## 2. 求最大最小值

```cpp
int mx=*max_element(v.begin(),v.end());

int mn=*min_element(v.begin(),v.end());
```

---

## 3. 求和

```cpp
int sum=accumulate(v.begin(),v.end(),0);
```

---

## 4. 判断存在

```cpp
if(find(v.begin(),v.end(),x)!=v.end())
{
    cout<<"存在";
}
```

---

# 第三章 algorithm（STL算法库，考试核心）

头文件：

```cpp
#include <algorithm>
```

部分数学算法需要：

```cpp
#include <numeric>
```

---

# 1. sort()

## 名称

```cpp
sort()
```

---

## 用法

基本格式：

```cpp
sort(开始位置,结束位置);
```

例如 vector：

```cpp
vector<int> v={3,1,5,2};

sort(v.begin(),v.end());
```

结果：

```
1 2 3 5
```

---

数组：

```cpp
int a[]={3,1,5,2};

sort(a,a+4);
```

结果：

```
1 2 3 5
```

---

降序：

格式：

```cpp
sort(begin,end,greater<类型>());
```

例如：

```cpp
sort(v.begin(),v.end(),greater<int>());
```

结果：

```
5 3 2 1
```

---

自定义排序：

格式：

```cpp
sort(begin,end,比较函数);
```

例如：

```cpp
bool cmp(int a,int b)
{
    return a>b;
}

sort(v.begin(),v.end(),cmp);
```

---

## 作用

排序。

考试最常用函数之一。

默认：

```
升序
```

---

# 2. stable_sort()

## 名称

```cpp
stable_sort()
```

---

## 用法

和sort一样：

```cpp
stable_sort(v.begin(),v.end());
```

---

## 作用

稳定排序。

区别：

sort：

相同元素顺序可能改变。

stable_sort：

保证相同元素原来的顺序。

考试：

普通排序直接sort。

---

# 3. reverse()

## 名称

```cpp
reverse()
```

---

## 用法

格式：

```cpp
reverse(开始,结束);
```

例如：

```cpp
vector<int> v={1,2,3,4};

reverse(v.begin(),v.end());
```

结果：

```
4 3 2 1
```

字符串：

```cpp
string s="hello";

reverse(s.begin(),s.end());
```

结果：

```
olleh
```

---

## 作用

反转顺序。

---

# 4. rotate()

## 名称

```cpp
rotate()
```

---

## 用法

格式：

```cpp
rotate(first,middle,last);
```

例如：

```cpp
vector<int> v={1,2,3,4,5};

rotate(v.begin(),v.begin()+2,v.end());
```

过程：

以第3个元素为开头：

```
3 4 5 1 2
```

---

## 作用

旋转数组。

例如：

左旋：

```
1 2 3 4 5

左移2位

3 4 5 1 2
```

---

# 5. swap()

## 名称

```cpp
swap()
```

---

## 用法

```cpp
swap(a,b);
```

例如：

```cpp
int a=10;
int b=20;

swap(a,b);
```

结果：

```
a=20
b=10
```

---

字符串：

```cpp
swap(s1,s2);
```

---

## 作用

交换两个变量。

---

# 6. max()

## 名称

```cpp
max()
```

---

## 用法

两个数：

```cpp
max(a,b);
```

例如：

```cpp
cout<<max(3,5);
```

输出：

```
5
```

---

多个：

```cpp
max({1,5,3});
```

输出：

```
5
```

---

## 作用

返回最大值。

---

# 7. min()

## 名称

```cpp
min()
```

---

## 用法

```cpp
min(a,b);
```

例如：

```cpp
cout<<min(3,5);
```

输出：

```
3
```

---

多个：

```cpp
min({3,1,5});
```

输出：

```
1
```

---

## 作用

返回最小值。

---

# 8. max_element()

## 名称

```cpp
max_element()
```

---

## 用法

格式：

```cpp
max_element(begin,end);
```

注意：

返回的是迭代器。

例如：

```cpp
vector<int> v={3,7,2};

auto it=max_element(v.begin(),v.end());

cout<<*it;
```

输出：

```
7
```

---

## 作用

找容器最大元素位置。

如果想得到下标：

```cpp
int pos=max_element(v.begin(),v.end())-v.begin();
```

---

# 9. min_element()

## 名称

```cpp
min_element()
```

---

## 用法

```cpp
auto it=min_element(v.begin(),v.end());
```

例如：

```cpp
cout<<*min_element(v.begin(),v.end());
```

输出：

```
2
```

---

## 作用

找最小元素。

---

# 10. find()

## 名称

```cpp
find()
```

---

## 用法

格式：

```cpp
find(begin,end,value);
```

例如：

```cpp
vector<int> v={1,2,3,4};

auto it=find(v.begin(),v.end(),3);
```

找到：

```
3
```

输出：

```cpp
cout<<*it;
```

---

不存在：

```cpp
if(it==v.end())
{
    cout<<"不存在";
}
```

---

## 作用

查找元素。

注意：

返回迭代器。

---

# 11. count()

## 名称

```cpp
count()
```

---

## 用法

格式：

```cpp
count(begin,end,value);
```

例如：

```cpp
vector<int> v={1,2,2,3};

int x=count(v.begin(),v.end(),2);
```

结果：

```
2
```

---

字符串：

```cpp
string s="hello";

cout<<count(s.begin(),s.end(),'l');
```

输出：

```
2
```

---

## 作用

统计元素出现次数。

---

# 12. accumulate()

头文件：

```cpp
#include <numeric>
```

## 名称

```cpp
accumulate()
```

---

## 用法

格式：

```cpp
accumulate(begin,end,初始值);
```

三个参数：

1. 开始位置
2. 结束位置
3. 初始值

例如：

```cpp
vector<int> v={1,2,3,4};

int sum=accumulate(v.begin(),v.end(),0);
```

计算：

```
0+1+2+3+4
```

结果：

```
10
```

---

注意：

第三个参数类型决定返回类型。

例如：

```cpp
vector<double> v={1.2,2.3};

double sum=accumulate(v.begin(),v.end(),0.0);
```

必须：

```
0.0
```

不能：

```
0
```

否则可能整数计算。

---

## 作用

求和。

---

# 13. fill()

## 名称

```cpp
fill()
```

---

## 用法

格式：

```cpp
fill(begin,end,value);
```

例如：

数组：

```cpp
int a[5];

fill(a,a+5,10);
```

结果：

```
10 10 10 10 10
```

vector：

```cpp
fill(v.begin(),v.end(),0);
```

---

## 作用

全部赋值。

---

# 14. replace()

## 名称

```cpp
replace()
```

---

## 用法

格式：

```cpp
replace(begin,end,旧值,新值);
```

例如：

```cpp
vector<int> v={1,2,2,3};

replace(v.begin(),v.end(),2,99);
```

结果：

```
1 99 99 3
```

---

字符串：

```cpp
string s="hello";

replace(s.begin(),s.end(),'l','x');
```

结果：

```
hexxo
```

---

## 作用

替换所有指定元素。

---

# 15. unique()

## 名称

```cpp
unique()
```

---

## 用法

常和sort一起：

```cpp
sort(v.begin(),v.end());

v.erase(unique(v.begin(),v.end()),v.end());
```

例如：

原：

```
1 2 2 3 3 3
```

unique：

```
1 2 3 ? ? ?
```

erase删除后：

```
1 2 3
```

---

## 作用

去除连续重复元素。

注意：

单独unique不会真正删除。

---

# 16. lower_bound()

## 名称

```cpp
lower_bound()
```

---

## 用法

要求：

数组必须有序。

格式：

```cpp
lower_bound(begin,end,value);
```

例如：

```cpp
vector<int> v={1,3,5,7};

auto it=lower_bound(v.begin(),v.end(),5);
```

结果：

```
5的位置
```

下标：

```cpp
int pos=it-v.begin();
```

输出：

```
2
```

---

## 作用

寻找第一个：

```
>= value
```

的位置。

例如：

数组：

```
1 3 5 7
```

找4：

返回：

```
5
```

---

# 17. upper_bound()

## 名称

```cpp
upper_bound()
```

---

## 用法

```cpp
upper_bound(v.begin(),v.end(),value);
```

例如：

```cpp
vector<int> v={1,3,5,7};

auto it=upper_bound(v.begin(),v.end(),5);
```

返回：

```
7
```

---

## 作用

寻找第一个：

```
> value
```

的位置。

---

# 18. binary_search()

## 名称

```cpp
binary_search()
```

---

## 用法

要求：

必须排序。

格式：

```cpp
binary_search(begin,end,value);
```

例如：

```cpp
vector<int> v={1,3,5,7};

bool flag=binary_search(v.begin(),v.end(),5);
```

结果：

```
true
```

---

## 作用

二分查找是否存在。

返回：

```
true/false
```

---

# 19. next_permutation()

## 名称

```cpp
next_permutation()
```

---

## 用法

```cpp
next_permutation(begin,end);
```

例如：

```cpp
string s="abc";

next_permutation(s.begin(),s.end());
```

结果：

```
acb
```

连续：

```cpp
while(next_permutation(s.begin(),s.end()))
{
    cout<<s;
}
```

输出所有排列。

---

## 作用

生成下一个字典序排列。

---

# 20. prev_permutation()

## 名称

```cpp
prev_permutation()
```

---

## 用法

```cpp
prev_permutation(s.begin(),s.end());
```

例如：

```
cba
```

变：

```
cab
```

---

## 作用

生成上一个排列。

---

# algorithm 高频考试组合

## 1. vector排序去重

```cpp
sort(v.begin(),v.end());

v.erase(unique(v.begin(),v.end()),v.end());
```

作用：

去除重复元素。

---

## 2. 求最大最小值

```cpp
int mx=*max_element(v.begin(),v.end());

int mn=*min_element(v.begin(),v.end());
```

---

## 3. 求和

```cpp
int sum=accumulate(v.begin(),v.end(),0);
```

---

## 4. 判断存在

```cpp
if(find(v.begin(),v.end(),x)!=v.end())
{
    cout<<"存在";
}
```

---

# 第四章 deque / stack / queue / priority_queue

---

# 一、deque（双端队列）

头文件：

```cpp
#include <deque>
```

---

## 1. deque定义

## 名称

```cpp
deque
```

## 用法

```cpp
deque<int> dq;
```

初始化：

```cpp
deque<int> dq={1,2,3};
```

## 作用

双端队列。

特点：

- 两端都可以插入
- 两端都可以删除
- 支持随机访问

类似：

```
vector + queue
```

---

# 2. push_back()

## 名称

```cpp
push_back()
```

## 用法

```cpp
dq.push_back(x);
```

例如：

```cpp
deque<int> dq;

dq.push_back(10);
dq.push_back(20);
```

结果：

```
10 20
```

## 作用

尾部添加元素。

---

# 3. push_front()

## 名称

```cpp
push_front()
```

## 用法

```cpp
dq.push_front(x);
```

例如：

```cpp
deque<int> dq={2,3};

dq.push_front(1);
```

结果：

```
1 2 3
```

## 作用

头部插入元素。

---

# 4. pop_back()

## 名称

```cpp
pop_back()
```

## 用法

```cpp
dq.pop_back();
```

例如：

```cpp
deque<int> dq={1,2,3};

dq.pop_back();
```

结果：

```
1 2
```

## 作用

删除最后元素。

---

# 5. pop_front()

## 名称

```cpp
pop_front()
```

## 用法

```cpp
dq.pop_front();
```

例如：

```cpp
deque<int> dq={1,2,3};

dq.pop_front();
```

结果：

```
2 3
```

## 作用

删除第一个元素。

---

# 6. front()

## 名称

```cpp
front()
```

## 用法

```cpp
dq.front();
```

例如：

```cpp
cout<<dq.front();
```

## 作用

访问第一个元素。

---

# 7. back()

## 名称

```cpp
back()
```

## 用法

```cpp
dq.back();
```

## 作用

访问最后元素。

---

# 8. size()

## 名称

```cpp
size()
```

## 用法

```cpp
dq.size();
```

## 作用

获取元素数量。

---

# 9. empty()

## 名称

```cpp
empty()
```

## 用法

```cpp
dq.empty();
```

## 作用

判断是否为空。

---

# 10. clear()

## 名称

```cpp
clear()
```

## 用法

```cpp
dq.clear();
```

## 作用

清空deque。

---

# 二、stack（栈）

头文件：

```cpp
#include <stack>
```

---

## 1. stack定义

## 名称

```cpp
stack
```

## 用法

```cpp
stack<int> st;
```

## 作用

先进后出：

```
LIFO
```

类似：

一摞盘子。

最后放的最先拿。

---

# 2. push()

## 名称

```cpp
push()
```

## 用法

```cpp
st.push(x);
```

例如：

```cpp
stack<int> st;

st.push(1);
st.push(2);
st.push(3);
```

栈：

```
3
2
1
```

## 作用

入栈。

---

# 3. pop()

## 名称

```cpp
pop()
```

## 用法

```cpp
st.pop();
```

例如：

```cpp
st.pop();
```

删除：

```
3
```

剩：

```
2
1
```

## 作用

删除栈顶元素。

注意：

pop没有返回值。

错误：

```cpp
int x=st.pop();
```

错误。

---

# 4. top()

## 名称

```cpp
top()
```

## 用法

```cpp
st.top();
```

例如：

```cpp
cout<<st.top();
```

## 作用

查看栈顶元素。

---

# 5. size()

## 名称

```cpp
size()
```

## 用法

```cpp
st.size();
```

## 作用

获取元素数量。

---

# 6. empty()

## 名称

```cpp
empty()
```

## 用法

```cpp
st.empty();
```

## 作用

判断栈是否为空。

---

## stack经典模板

### 括号匹配

```cpp
string s;

stack<char> st;

for(char c:s)
{
    if(c=='(')
        st.push(c);

    else if(c==')')
    {
        if(st.empty())
            cout<<"NO";

        st.pop();
    }
}
```

作用：

判断括号是否合法。

---

# 三、queue（普通队列）

头文件：

```cpp
#include <queue>
```

---

# 1. queue定义

## 名称

```cpp
queue
```

## 用法

```cpp
queue<int> q;
```

## 作用

先进先出：

```
FIFO
```

类似排队：

先来先走。

---

# 2. push()

## 名称

```cpp
push()
```

## 用法

```cpp
q.push(x);
```

例如：

```cpp
q.push(1);
q.push(2);
q.push(3);
```

队列：

```
1 2 3
```

## 作用

队尾加入元素。

---

# 3. pop()

## 名称

```cpp
pop()
```

## 用法

```cpp
q.pop();
```

例如：

原：

```
1 2 3
```

执行：

```cpp
q.pop();
```

结果：

```
2 3
```

## 作用

删除队头元素。

注意：

没有返回值。

---

# 4. front()

## 名称

```cpp
front()
```

## 用法

```cpp
q.front();
```

例如：

```cpp
cout<<q.front();
```

输出：

队头元素。

## 作用

访问队头。

---

# 5. back()

## 名称

```cpp
back()
```

## 用法

```cpp
q.back();
```

## 作用

访问队尾。

---

# 6. size()

## 名称

```cpp
size()
```

## 用法

```cpp
q.size();
```

## 作用

元素数量。

---

# 7. empty()

## 名称

```cpp
empty()
```

## 用法

```cpp
q.empty();
```

## 作用

判断队列为空。

---

# 四、priority_queue（优先队列）

头文件：

```cpp
#include <queue>
```

---

# 1. priority_queue定义

## 名称

```cpp
priority_queue
```

## 用法

默认：

```cpp
priority_queue<int> pq;
```

## 作用

自动排序的队列。

默认：

大根堆：

```
最大值永远在顶部
```

---

# 2. push()

## 名称

```cpp
push()
```

## 用法

```cpp
pq.push(x);
```

例如：

```cpp
priority_queue<int> pq;

pq.push(3);
pq.push(10);
pq.push(5);
```

内部：

```
10
5
3
```

---

# 3. top()

## 名称

```cpp
top()
```

## 用法

```cpp
pq.top();
```

例如：

```cpp
cout<<pq.top();
```

输出：

```
10
```

## 作用

查看最大元素。

---

# 4. pop()

## 名称

```cpp
pop()
```

## 用法

```cpp
pq.pop();
```

删除：

当前最大值。

---

# 5. empty()

## 名称

```cpp
empty()
```

## 用法

```cpp
pq.empty();
```

## 作用

判断为空。

---

# 6. size()

## 名称

```cpp
size()
```

## 用法

```cpp
pq.size();
```

## 作用

元素数量。

---

# 7. 小根堆

## 名称

小根priority_queue

## 用法

```cpp
priority_queue<int,vector<int>,greater<int>> pq;
```

例如：

```cpp
pq.push(5);
pq.push(2);
pq.push(8);
```

top：

```
2
```

---

## 作用

让最小值在顶部。

考试非常重要。

---

# priority_queue自定义排序

例如：

结构体：

```cpp
struct Node
{
    int x;
    int y;
};
```

比较：

```cpp
struct cmp
{
    bool operator()(Node a,Node b)
    {
        return a.x>b.x;
    }
};
```

定义：

```cpp
priority_queue<Node,vector<Node>,cmp> pq;
```

作用：

复杂排序。

---

# 五、四种容器总结（考试记忆）

| 容器           | 特点     | 常用函数             |
| -------------- | -------- | -------------------- |
| vector         | 动态数组 | push_back erase sort |
| deque          | 两端操作 | push_front           |
| stack          | 后进先出 | push pop top         |
| queue          | 先进先出 | push pop front       |
| priority_queue | 自动排序 | push top pop         |

---

# 第五章 set / multiset / map / unordered_map（STL核心）

这一章非常重要。

考试中常见：

- 去重
- 查找某个元素是否存在
- 统计次数
- 建立映射关系
- 字符频率统计

---

# 一、set（集合）

头文件：

```cpp
#include <set>
```

---

# 1. set定义

## 名称

```cpp
set
```

## 用法

```cpp
set<int> s;
```

初始化：

```cpp
set<int> s={3,1,2,2};
```

结果：

```
1 2 3
```

特点：

1. 自动排序
2. 自动去重

---

## 作用

保存唯一元素。

类似数学集合。

---

# 2. insert()

## 名称

```cpp
insert()
```

## 用法

```cpp
s.insert(value);
```

例如：

```cpp
set<int> s;

s.insert(3);
s.insert(1);
s.insert(3);
```

结果：

```
1 3
```

## 作用

插入元素。

注意：

重复元素不会加入。

---

# 3. erase()

## 名称

```cpp
erase()
```

## 用法1：删除指定元素

```cpp
s.erase(value);
```

例如：

```cpp
set<int> s={1,2,3};

s.erase(2);
```

结果：

```
1 3
```

---

## 用法2：删除迭代器位置

```cpp
auto it=s.begin();

s.erase(it);
```

删除第一个元素。

---

## 用法3：删除区间

```cpp
s.erase(s.begin(),s.end());
```

清空。

---

## 作用

删除元素。

---

# 4. find()

## 名称

```cpp
find()
```

## 用法

```cpp
s.find(value);
```

例如：

```cpp
set<int> s={1,2,3};

auto it=s.find(2);
```

如果存在：

返回位置。

不存在：

```cpp
s.end()
```

判断：

```cpp
if(s.find(2)!=s.end())
{
    cout<<"存在";
}
```

## 作用

查找元素。

---

# 5. count()

## 名称

```cpp
count()
```

## 用法

```cpp
s.count(value);
```

例如：

```cpp
set<int> s={1,2,3};

cout<<s.count(2);
```

输出：

```
1
```

不存在：

```
0
```

## 作用

判断元素是否存在。

因为set没有重复：

返回只能：

```
0 或 1
```

---

# 6. size()

## 名称

```cpp
size()
```

## 用法

```cpp
s.size();
```

## 作用

元素数量。

---

# 7. empty()

## 名称

```cpp
empty()
```

## 用法

```cpp
s.empty();
```

## 作用

判断为空。

---

# 8. begin()

## 名称

```cpp
begin()
```

## 用法

```cpp
auto it=s.begin();
```

例如：

```cpp
cout<<*s.begin();
```

输出：

最小元素。

---

# 9. end()

## 名称

```cpp
end()
```

## 用法

```cpp
s.end();
```

作用：

最后一个元素之后。

---

# 10. lower_bound()

## 名称

```cpp
lower_bound()
```

## 用法

```cpp
auto it=s.lower_bound(x);
```

例如：

```cpp
set<int> s={1,3,5,7};

auto it=s.lower_bound(4);
```

返回：

```
5
```

## 作用

找第一个：

```
>=x
```

---

# 11. upper_bound()

## 名称

```cpp
upper_bound()
```

## 用法

```cpp
auto it=s.upper_bound(x);
```

例如：

```cpp
auto it=s.upper_bound(5);
```

返回：

```
7
```

## 作用

找第一个：

```
>x
```

---

# set遍历

## 方法1：范围for

```cpp
for(auto x:s)
{
    cout<<x;
}
```

输出：

```
1 2 3
```

---

## 方法2：迭代器

```cpp
for(auto it=s.begin();it!=s.end();it++)
{
    cout<<*it;
}
```

---

# set常用场景

## 去重排序

原：

```cpp
vector<int> v={3,1,2,2};
```

方法：

```cpp
set<int> s(v.begin(),v.end());
```

结果：

```
1 2 3
```

---

---

# 二、multiset（可重复集合）

头文件：

```cpp
#include <set>
```

---

# 1. multiset定义

## 名称

```cpp
multiset
```

## 用法

```cpp
multiset<int> ms;
```

例如：

```cpp
multiset<int> ms={1,2,2,3};
```

结果：

```
1 2 2 3
```

---

## 作用

允许重复元素的set。

区别：

|      | set    | multiset |
| ---- | ------ | -------- |
| 重复 | 不允许 | 允许     |
| 排序 | 自动   | 自动     |

---

# 2. insert()

## 用法

```cpp
ms.insert(x);
```

例如：

```cpp
ms.insert(2);
ms.insert(2);
```

结果：

```
2 2
```

---

# 3. erase()

重点！

## 删除一个

```cpp
auto it=ms.find(2);

ms.erase(it);
```

删除一个2。

---

## 删除全部

```cpp
ms.erase(2);
```

所有2都会删除。

---

## 作用

删除元素。

---

# 4. count()

## 用法

```cpp
ms.count(x);
```

例如：

```cpp
multiset<int> ms={1,2,2,2,3};

cout<<ms.count(2);
```

输出：

```
3
```

## 作用

统计出现次数。

---

# 三、map（映射）

头文件：

```cpp
#include <map>
```

---

# 1. map定义

## 名称

```cpp
map
```

## 用法

```cpp
map<string,int> mp;
```

表示：

```
字符串 -> 整数
```

例如：

```cpp
mp["apple"]=5;
```

形成：

```
apple 5
```

---

## 作用

键值映射。

类似：

字典：

```
单词 → 解释
姓名 → 成绩
```

---

# 2. 插入元素

## 方法1：下标

```cpp
mp[key]=value;
```

例如：

```cpp
map<string,int> mp;

mp["Tom"]=90;
```

---

## 方法2：insert

```cpp
mp.insert(pair<string,int>("Tom",90));
```

或者：

```cpp
mp.insert({"Tom",90});
```

---

## 作用

添加键值对。

---

# 3. 访问元素

## 名称

```cpp
[]
```

## 用法

```cpp
mp[key];
```

例如：

```cpp
cout<<mp["Tom"];
```

输出：

```
90
```

---

注意：

如果key不存在：

```cpp
mp["Bob"];
```

会自动创建：

```
Bob 0
```

---

# 4. erase()

## 用法

删除key：

```cpp
mp.erase(key);
```

例如：

```cpp
mp.erase("Tom");
```

删除：

```
Tom
```

---

## 作用

删除映射。

---

# 5. find()

## 用法

```cpp
mp.find(key);
```

例如：

```cpp
if(mp.find("Tom")!=mp.end())
{
    cout<<"存在";
}
```

## 作用

查找key。

---

# 6. count()

## 用法

```cpp
mp.count(key);
```

例如：

```cpp
mp.count("Tom");
```

返回：

```
1
```

不存在：

```
0
```

---

# 7. size()

## 用法

```cpp
mp.size();
```

作用：

键值对数量。

---

# 8. 遍历map（重点）

## 名称

```cpp
pair
```

## 用法

```cpp
for(auto p:mp)
{
    cout<<p.first<<" "<<p.second;
}
```

例如：

map：

```
Tom 90
Bob 80
```

输出：

```
Tom 90
Bob 80
```

---

其中：

```cpp
p.first
```

表示：

key

```cpp
p.second
```

表示：

value

---

# 9. map自动排序

默认：

按照key升序。

例如：

```cpp
map<int,string> mp;

mp[3]="c";
mp[1]="a";
mp[2]="b";
```

遍历：

```
1 a
2 b
3 c
```

---

# map经典用途

## 统计次数

例如：

统计字符串字符：

```cpp
string s="hello";

map<char,int> mp;


for(char c:s)
{
    mp[c]++;
}
```

结果：

```
h 1
e 1
l 2
o 1
```

---

# 四、unordered_map（哈希表）

头文件：

```cpp
#include <unordered_map>
```

---

# 1. unordered_map定义

## 名称

```cpp
unordered_map
```

## 用法

```cpp
unordered_map<string,int> mp;
```

---

## 作用

类似map。

区别：

|      | map      | unordered_map |
| ---- | -------- | ------------- |
| 底层 | 红黑树   | 哈希表        |
| 排序 | 自动排序 | 无序          |
| 速度 | O(logn)  | 平均O(1)      |

---

# 2. 插入

```cpp
mp[key]=value;
```

例如：

```cpp
mp["apple"]=10;
```

---

# 3. 查找

```cpp
mp.find(key);
```

例如：

```cpp
if(mp.find("apple")!=mp.end())
{
    cout<<"yes";
}
```

---

# 4. 统计次数

最常用：

```cpp
unordered_map<char,int> cnt;


for(char c:s)
{
    cnt[c]++;
}
```

---

# map和unordered_map选择

考试：

需要排序：

↓

```cpp
map
```

只需要快速查找：

↓

```cpp
unordered_map
```

统计次数：

↓

```cpp
unordered_map
```

---

# 第六章 pair / tuple / iterator（迭代器）

这一章属于 STL 基础连接部分。

很多 STL 函数：

```cpp
sort()
find()
lower_bound()
map遍历
```

都会用到。

---

# 一、pair（存两个数据）

头文件：

```cpp
#include <utility>
```

通常：

```cpp
#include <bits/stdc++.h>
```

已经包含。

---

# 1. pair定义

## 名称

```cpp
pair
```

## 用法

格式：

```cpp
pair<类型1,类型2> 名称;
```

例如：

```cpp
pair<int,string> p;
```

表示：

存：

```text
整数 + 字符串
```

---

初始化：

```cpp
pair<int,string> p={1,"Tom"};
```

或者：

```cpp
pair<int,string> p;

p.first=1;
p.second="Tom";
```

---

## 作用

把两个变量绑定在一起。

例如：

学生：

```
学号 + 姓名
```

坐标：

```
x + y
```

---

# 2. first

## 名称

```cpp
first
```

## 用法

```cpp
p.first;
```

例如：

```cpp
pair<int,string> p={100,"Tom"};

cout<<p.first;
```

输出：

```
100
```

## 作用

访问pair第一个元素。

---

# 3. second

## 名称

```cpp
second
```

## 用法

```cpp
p.second;
```

例如：

```cpp
cout<<p.second;
```

输出：

```
Tom
```

## 作用

访问第二个元素。

---

# 4. make_pair()

## 名称

```cpp
make_pair()
```

## 用法

格式：

```cpp
make_pair(数据1,数据2);
```

例如：

```cpp
auto p=make_pair(10,"hello");
```

等价：

```cpp
pair<int,string> p={10,"hello"};
```

---

## 作用

快速创建pair。

---

# 5. pair比较

pair可以直接比较。

例如：

```cpp
pair<int,int> a={1,2};

pair<int,int> b={1,3};


cout<<(a<b);
```

比较规则：

先比较：

```text
first
```

如果相同：

比较：

```text
second
```

例如：

```
(1,2)
(1,3)
```

因为：

```
2 < 3
```

所以：

```
a < b
```

---

## 作用

常用于排序。

---

# 6. pair排序

例如：

```cpp
vector<pair<int,int>> v;

v.push_back({2,5});
v.push_back({1,3});
v.push_back({2,1});


sort(v.begin(),v.end());
```

结果：

```
(1,3)
(2,1)
(2,5)
```

规则：

第一关键字升序。

第二关键字升序。

---

# 7. 自定义pair排序

例如：

按照second排序：

```cpp
bool cmp(pair<int,int>a,pair<int,int>b)
{
    return a.second<b.second;
}


sort(v.begin(),v.end(),cmp);
```

作用：

改变排序规则。

---

# 二、tuple（三个及以上数据）

头文件：

```cpp
#include <tuple>
```

---

# 1. tuple定义

## 名称

```cpp
tuple
```

## 用法

```cpp
tuple<int,string,double> t;
```

表示：

```
int
string
double
```

三个数据。

---

初始化：

```cpp
tuple<int,string,double> t
=
{
1,
"Tom",
99.5
};
```

---

## 作用

存储多个不同类型的数据。

pair只能两个。

tuple可以多个。

---

# 2. get()

## 名称

```cpp
get<>
```

## 用法

格式：

```cpp
get<编号>(tuple);
```

例如：

```cpp
tuple<int,string,double> t={1,"Tom",99.5};


cout<<get<0>(t);
```

输出：

```
1
```

```cpp
cout<<get<1>(t);
```

输出：

```
Tom
```

---

注意：

编号从0开始。

```
get<0>
第一个

get<1>
第二个

get<2>
第三个
```

---

## 作用

访问tuple元素。

---

# 三、iterator（迭代器）

iterator 是 STL 的核心。

简单理解：

迭代器 = 指向容器元素的指针。

类似：

C语言：

```cpp
int *p;
```

STL：

```cpp
vector<int>::iterator it;
```

---

# 1. iterator定义

## 名称

```cpp
iterator
```

## 用法

vector：

```cpp
vector<int>::iterator it;
```

例如：

```cpp
vector<int> v={1,2,3};

vector<int>::iterator it;
```

---

现在推荐：

```cpp
auto it;
```

例如：

```cpp
auto it=v.begin();
```

---

## 作用

遍历容器。

---

# 2. begin()

## 名称

```cpp
begin()
```

## 用法

```cpp
auto it=v.begin();
```

例如：

```cpp
vector<int> v={1,2,3};

auto it=v.begin();

cout<<*it;
```

输出：

```
1
```

---

## 作用

返回第一个元素位置。

---

# 3. end()

## 名称

```cpp
end()
```

## 用法

```cpp
auto it=v.end();
```

注意：

end不是最后元素。

它指向：

```
最后一个元素后面
```

所以：

错误：

```cpp
cout<<*v.end();
```

正确：

```cpp
cout<<*(v.end()-1);
```

---

## 作用

表示遍历结束。

---

# 4. 解引用 \*

## 名称

```cpp
*
```

## 用法

```cpp
*iterator
```

例如：

```cpp
auto it=v.begin();

cout<<*it;
```

输出：

```
第一个元素
```

---

## 作用

获得迭代器指向的数据。

---

# 5. iterator移动

## 名称

++

--

## 用法

向后：

```cpp
it++;
```

向前：

```cpp
it--;
```

例如：

```cpp
auto it=v.begin();

it++;

cout<<*it;
```

输出：

第二个元素。

---

# 6. iterator遍历vector

## 方法

```cpp
for(auto it=v.begin();
    it!=v.end();
    it++)
{
    cout<<*it;
}
```

输出：

```
123
```

---

# 7. iterator遍历set

set不能：

```cpp
s[0]
```

只能：

```cpp
for(auto it=s.begin();
    it!=s.end();
    it++)
{
    cout<<*it;
}
```

---

# 8. iterator遍历map（重点）

map里面：

元素类型：

```cpp
pair
```

例如：

```cpp
map<string,int> mp;


mp["Tom"]=90;
mp["Bob"]=80;


for(auto it=mp.begin();
    it!=mp.end();
    it++)
{
    cout<<it->first;
    cout<<it->second;
}
```

注意：

这里：

```cpp
it->first
```

等价：

```cpp
(*it).first
```

---

# 9. 箭头符号 ->

## 名称

成员访问

## 用法

如果：

```cpp
it
```

是指针：

访问成员：

```cpp
it->成员
```

例如：

map：

```cpp
it->first
it->second
```

---

## 作用

访问指针对象成员。

---

# 10. const_iterator

## 名称

```cpp
const_iterator
```

## 用法

```cpp
vector<int>::const_iterator it;
```

或者：

```cpp
auto it=v.cbegin();
```

---

## 作用

只读迭代器。

不能：

```cpp
*it=10;
```

---

# 11. reverse_iterator

## 名称

反向迭代器

## 用法

```cpp
auto it=v.rbegin();
```

遍历：

```cpp
for(auto it=v.rbegin();
    it!=v.rend();
    it++)
{
    cout<<*it;
}
```

例如：

vector:

```
1 2 3
```

输出：

```
321
```

---

## 作用

反向遍历。

---

# 四、auto关键字（STL必用）

## 名称

```cpp
auto
```

## 用法

不用写复杂类型：

传统：

```cpp
vector<int>::iterator it;
```

auto：

```cpp
auto it=v.begin();
```

---

map：

传统：

```cpp
map<string,int>::iterator it;
```

auto：

```cpp
auto it=mp.begin();
```

---

## 作用

让编译器自动推断类型。

考试非常推荐。

---

# 五、范围for循环（C++11）

## 名称

range-based for

## 用法

vector：

```cpp
for(auto x:v)
{
    cout<<x;
}
```

string：

```cpp
for(char c:s)
{
    cout<<c;
}
```

map：

```cpp
for(auto p:mp)
{
    cout<<p.first;
    cout<<p.second;
}
```

---

## 作用

快速遍历。

---

# 六、STL常用头文件总表（考试）

| 功能           | 头文件          |
| -------------- | --------------- |
| vector         | string          |
| stack          | `<stack>`       |
| queue          | `<queue>`       |
| priority_queue | `<queue>`       |
| set/map        | `<set>` `<map>` |
| 算法           | `<algorithm>`   |
| 累加           | `<numeric>`     |
| pair           | `<utility>`     |
| tuple          | `<tuple>`       |

---

# 第七章 C++ STL上机考试万能模板

这一部分不是介绍函数，而是整理**考试直接能套用的代码模板**。

---

# 1. 万能头文件模板

## 名称

```cpp
#include <bits/stdc++.h>
```

## 用法

```cpp
#include <bits/stdc++.h>
using namespace std;

int main()
{


    return 0;
}
```

## 作用

一次包含几乎所有 STL：

包括：

```cpp
vector
string
map
set
queue
stack
algorithm
numeric
```

等。

考试最推荐。

---

# 2. 快速输入输出

## 名称

```cpp
ios::sync_with_stdio(false);
cin.tie(nullptr);
```

## 用法

```cpp
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);


}
```

## 作用

关闭 C 和 C++ 输入同步，提高速度。

普通题可以不写。

数据量大建议写。

---

# 3. vector输入模板

## 场景

输入n个数字。

## 写法

```cpp
int n;

cin>>n;


vector<int> v(n);


for(int i=0;i<n;i++)
{
    cin>>v[i];
}
```

## 作用

快速创建数组。

---

另一种：

```cpp
vector<int> v;


for(int i=0;i<n;i++)
{
    int x;

    cin>>x;

    v.push_back(x);
}
```

---

区别：

| 方法      | 特点         |
| --------- | ------------ |
| vector(n) | 提前知道数量 |
| push_back | 不知道数量   |

---

# 4. vector遍历模板

## 方法1：下标

```cpp
for(int i=0;i<v.size();i++)
{
    cout<<v[i]<<" ";
}
```

适合：

需要下标。

---

## 方法2：范围for

```cpp
for(auto x:v)
{
    cout<<x<<" ";
}
```

适合：

只读取。

---

## 方法3：引用

```cpp
for(auto &x:v)
{
    cin>>x;
}
```

作用：

直接修改原元素。

例如：

```cpp
for(auto &x:v)
{
    x*=2;
}
```

---

# 5. 数组排序模板

## 升序

```cpp
sort(a,a+n);
```

例如：

```cpp
int a[5]={3,1,5,2,4};

sort(a,a+5);
```

结果：

```
1 2 3 4 5
```

---

## vector排序

```cpp
sort(v.begin(),v.end());
```

---

## 降序

数组：

```cpp
sort(a,a+n,greater<int>());
```

vector：

```cpp
sort(v.begin(),v.end(),greater<int>());
```

---

# 6. 字符串排序模板

## 名称

字符排序

## 用法

```cpp
string s="dcba";


sort(s.begin(),s.end());
```

结果：

```
abcd
```

---

降序：

```cpp
sort(s.begin(),s.end(),greater<char>());
```

结果：

```
dcba
```

---

# 7. vector求最大最小值

## 最大值

```cpp
int mx=*max_element(v.begin(),v.end());
```

---

## 最小值

```cpp
int mn=*min_element(v.begin(),v.end());
```

---

## 最大值位置

```cpp
int pos=max_element(v.begin(),v.end())-v.begin();
```

例如：

```cpp
v={3,7,2}
```

返回：

```
1
```

---

# 8. vector求和模板

## 名称

accumulate

## 用法

```cpp
int sum=accumulate(
    v.begin(),
    v.end(),
    0
);
```

例如：

```cpp
v={1,2,3}
```

结果：

```
6
```

---

# 9. 去重模板

## 场景

数组：

```
1 2 2 3 3
```

变：

```
1 2 3
```

## 写法

```cpp
sort(v.begin(),v.end());


v.erase(
    unique(v.begin(),v.end()),
    v.end()
);
```

步骤：

第一步：

排序：

```
1 2 2 3 3
```

第二步：

unique：

```
1 2 3 ? ?
```

第三步：

erase删除。

---

# 10. 查找元素模板

## 方法1：find

```cpp
auto it=find(
    v.begin(),
    v.end(),
    x
);


if(it!=v.end())
{
    cout<<"找到";
}
```

---

## 方法2：count

```cpp
if(count(v.begin(),v.end(),x))
{
    cout<<"存在";
}
```

---

# 11. set去重模板

## 场景

自动排序去重。

代码：

```cpp
set<int> s;


for(int x:v)
{
    s.insert(x);
}
```

遍历：

```cpp
for(auto x:s)
{
    cout<<x<<" ";
}
```

---

# 12. map统计次数模板（超级高频）

## 场景

统计：

- 字符出现次数
- 数字出现次数
- 单词次数

代码：

```cpp
map<int,int> mp;


for(int x:v)
{
    mp[x]++;
}
```

例如：

输入：

```
1 2 2 3
```

结果：

```
1 -> 1次
2 -> 2次
3 -> 1次
```

---

字符串：

```cpp
string s="hello";


map<char,int> mp;


for(char c:s)
{
    mp[c]++;
}
```

结果：

```
h 1
e 1
l 2
o 1
```

---

# 13. unordered_map统计模板

推荐：

```cpp
unordered_map<int,int> mp;


for(int x:v)
{
    mp[x]++;
}
```

速度更快。

---

# 14. map遍历模板

## 方法1

```cpp
for(auto p:mp)
{
    cout<<p.first<<" ";

    cout<<p.second;
}
```

---

## 方法2（修改）

```cpp
for(auto &p:mp)
{
    p.second++;
}
```

---

# 15. stack括号匹配模板

## 场景

判断：

```
(())
```

是否合法。

代码：

```cpp
string s;


cin>>s;


stack<char> st;


for(char c:s)
{
    if(c=='(')
    {
        st.push(c);
    }


    else
    {
        if(st.empty())
        {
            cout<<"NO";

            return 0;
        }


        st.pop();
    }
}


if(st.empty())
    cout<<"YES";

else
    cout<<"NO";
```

---

# 16. queue BFS模板

## 场景

图搜索：

- 迷宫
- 最短路径
- 层序遍历

模板：

```cpp
queue<int> q;


q.push(start);


while(!q.empty())
{
    int x=q.front();

    q.pop();


    //处理x


}
```

---

# 17. priority_queue最大值模板

## 默认大根堆

```cpp
priority_queue<int> pq;


pq.push(5);

pq.push(10);

pq.push(3);


cout<<pq.top();
```

输出：

```
10
```

---

# 18. priority_queue小根堆

```cpp
priority_queue<
    int,
    vector<int>,
    greater<int>
> pq;
```

例如：

```cpp
pq.push(5);
pq.push(2);
pq.push(8);
```

top：

```
2
```

---

# 19. 二分查找模板

前提：

必须排序。

---

## lower_bound

```cpp
auto it=
lower_bound(
    v.begin(),
    v.end(),
    x
);


int pos=it-v.begin();
```

寻找：

```
>=x
```

---

## upper_bound

```cpp
auto it=
upper_bound(
    v.begin(),
    v.end(),
    x
);
```

寻找：

```
>x
```

---

# 20. 全排列模板

## next_permutation

例如：

```cpp
string s="abc";


do
{
    cout<<s<<endl;

}
while(next_permutation(
    s.begin(),
    s.end()
));
```

输出：

```
abc
acb
bac
bca
cab
cba
```

---

# 21. 二维vector模板

## 定义

```cpp
vector<vector<int>> v(
    n,
    vector<int>(m)
);
```

创建：

n行m列。

例如：

```cpp
vector<vector<int>> a(
    3,
    vector<int>(4,0)
);
```

得到：

3×4 全0矩阵。

---

# 22. 二维vector遍历

```cpp
for(int i=0;i<v.size();i++)
{
    for(int j=0;j<v[i].size();j++)
    {
        cout<<v[i][j];
    }
}
```

---

# 23. pair排序模板

## 默认

```cpp
vector<pair<int,int>> v;


sort(v.begin(),v.end());
```

排序：

第一关键字

↓

第二关键字。

---

## 按第二关键字

```cpp
sort(
v.begin(),
v.end(),
[](pair<int,int>a,pair<int,int>b)
{
    return a.second<b.second;
});
```

---

# 24. 常用字符串处理组合

## 删除某字符

例如删除空格：

```cpp
s.erase(
remove(
s.begin(),
s.end(),
' '
),
s.end()
);
```

---

## 字符串反转

```cpp
reverse(
s.begin(),
s.end()
);
```

---

## 字符串转数字

```cpp
int x=stoi(s);
```

---

## 数字转字符串

```cpp
string s=to_string(x);
```

---

# 25. STL复杂度速记

| 操作                | 复杂度   |
| ------------------- | -------- |
| vector尾插push_back | O(1)     |
| vector中间插入      | O(n)     |
| sort                | O(nlogn) |
| find(vector)        | O(n)     |
| binary_search       | O(logn)  |
| set查找             | O(logn)  |
| map查找             | O(logn)  |
| unordered_map查找   | 平均O(1) |
| priority_queue插入  | O(logn)  |

---

# 26. 考试最常用TOP20函数（建议背）

```cpp
vector:
push_back()
pop_back()
size()
erase()
insert()
clear()
begin()
end()


string:
getline()
size()
substr()
find()
erase()
replace()
append()


algorithm:
sort()
reverse()
find()
count()
accumulate()
max_element()
min_element()
lower_bound()
upper_bound()


map:
insert
[]
find
count


stack:
push
pop
top


queue:
push
pop
front


priority_queue:
push
pop
top
```

---

# 第八章 STL补充知识（容易遗漏但考试可能出现）

这一章主要补充：

- array
- unordered_set
- bitset
- numeric更多函数
- functional
- lambda表达式
- 自定义比较器
- STL常见坑

---

# 一、array（固定大小数组）

头文件：

```cpp
#include <array>
```

---

# 1. array定义

## 名称

```cpp
array
```

## 用法

格式：

```cpp
array<类型,大小> 名称;
```

例如：

```cpp
array<int,5> a;
```

表示：

```text
长度固定为5的int数组
```

初始化：

```cpp
array<int,5> a={1,2,3,4,5};
```

---

## 作用

STL版本的普通数组。

区别：

普通数组：

```cpp
int a[5];
```

array：

```cpp
array<int,5> a;
```

优点：

- 支持STL函数
- 有size()
- 有迭代器

---

# 2. size()

## 名称

```cpp
size()
```

## 用法

```cpp
a.size();
```

例如：

```cpp
array<int,5> a;

cout<<a.size();
```

输出：

```text
5
```

## 作用

获取数组大小。

---

# 3. front()

## 用法

```cpp
a.front();
```

例如：

```cpp
cout<<a.front();
```

作用：

访问第一个元素。

---

# 4. back()

## 用法

```cpp
a.back();
```

作用：

访问最后元素。

---

# 5. fill()

## 用法

```cpp
a.fill(0);
```

例如：

```cpp
array<int,5> a;

a.fill(10);
```

结果：

```text
10 10 10 10 10
```

## 作用

全部赋值。

---

# 6. begin()

## 用法

```cpp
a.begin();
```

作用：

返回首迭代器。

---

# 7. end()

## 用法

```cpp
a.end();
```

作用：

返回尾后迭代器。

---

# 二、unordered_set（哈希集合）

头文件：

```cpp
#include <unordered_set>
```

---

# 1. unordered_set定义

## 名称

```cpp
unordered_set
```

## 用法

```cpp
unordered_set<int> s;
```

---

## 作用

类似set。

区别：

|      | set      | unordered_set |
| ---- | -------- | ------------- |
| 底层 | 红黑树   | 哈希表        |
| 排序 | 自动排序 | 无序          |
| 速度 | O(logn)  | 平均O(1)      |

---

# 2. insert()

## 用法

```cpp
s.insert(x);
```

例如：

```cpp
unordered_set<int> s;

s.insert(3);
s.insert(1);
```

输出顺序不固定。

---

# 3. find()

## 用法

```cpp
auto it=s.find(x);
```

判断：

```cpp
if(it!=s.end())
{
    cout<<"存在";
}
```

---

# 4. count()

## 用法

```cpp
s.count(x);
```

返回：

存在：

```text
1
```

不存在：

```text
0
```

---

# 5. erase()

## 用法

```cpp
s.erase(x);
```

作用：

删除元素。

---

# 6. size()

## 用法

```cpp
s.size();
```

作用：

元素数量。

---

# 三、bitset（二进制位操作）

头文件：

```cpp
#include <bitset>
```

---

# 1. bitset定义

## 名称

```cpp
bitset
```

## 用法

```cpp
bitset<8> b;
```

表示：

8位二进制。

初始化：

```cpp
bitset<8> b("1010");
```

结果：

```text
00001010
```

---

## 作用

处理二进制非常方便。

例如：

- 位运算
- 状态压缩

---

# 2. count()

## 用法

```cpp
b.count();
```

例如：

```cpp
bitset<8> b("1011");

cout<<b.count();
```

输出：

```text
3
```

## 作用

统计1的数量。

---

# 3. size()

## 用法

```cpp
b.size();
```

作用：

位数。

---

# 4. test()

## 名称

```cpp
test()
```

## 用法

```cpp
b.test(pos);
```

例如：

```cpp
bitset<8> b("1010");

cout<<b.test(1);
```

作用：

判断第pos位是否为1。

---

# 5. set()

## 用法

全部置1：

```cpp
b.set();
```

指定位置：

```cpp
b.set(2);
```

作用：

把某位变成1。

---

# 6. reset()

## 用法

全部置0：

```cpp
b.reset();
```

指定位置：

```cpp
b.reset(2);
```

作用：

把某位变成0。

---

# 7. flip()

## 用法

全部取反：

```cpp
b.flip();
```

指定：

```cpp
b.flip(2);
```

例如：

0→1

1→0

---

# 8. to_string()

## 用法

```cpp
string s=b.to_string();
```

作用：

转换成字符串。

---

# 9. to_ulong()

## 用法

```cpp
unsigned long x=b.to_ulong();
```

作用：

转换成整数。

---

# 四、numeric头文件其他函数

头文件：

```cpp
#include <numeric>
```

---

# 1. accumulate()

前面讲过：

```cpp
accumulate(
begin,
end,
初始值
);
```

作用：

求和。

---

# 2. iota()

## 名称

```cpp
iota()
```

## 用法

```cpp
iota(
begin,
end,
初始值
);
```

例如：

```cpp
vector<int> v(5);


iota(
v.begin(),
v.end(),
1
);
```

结果：

```text
1 2 3 4 5
```

---

## 作用

连续赋值。

类似：

```cpp
for循环赋值
```

---

# 3. inner_product()

## 名称

```cpp
inner_product()
```

## 用法

```cpp
inner_product(
a.begin(),
a.end(),
b.begin(),
0
);
```

例如：

a:

```
1 2 3
```

b:

```
4 5 6
```

计算：

```
1*4+2*5+3*6
```

结果：

```
32
```

---

## 作用

计算向量点积。

---

# 4. partial_sum()

## 名称

```cpp
partial_sum()
```

## 用法

```cpp
partial_sum(
begin,
end,
输出位置
);
```

例如：

```cpp
vector<int> v={1,2,3,4};

vector<int> ans(4);


partial_sum(
v.begin(),
v.end(),
ans.begin()
);
```

结果：

```
1 3 6 10
```

---

## 作用

前缀和。

---

# 5. adjacent_difference()

## 名称

```cpp
adjacent_difference()
```

## 用法

```cpp
adjacent_difference(
begin,
end,
输出位置
);
```

例如：

输入：

```
1 3 6 10
```

输出：

```
1 2 3 4
```

---

## 作用

求相邻差。

---

# 五、functional（函数对象）

头文件：

```cpp
#include <functional>
```

常用于：

sort第三个参数。

---

# 1. greater()

## 名称

```cpp
greater<>
```

## 用法

降序：

```cpp
sort(
v.begin(),
v.end(),
greater<int>()
);
```

作用：

从大到小排序。

---

# 2. less()

默认：

```cpp
less<int>()
```

用法：

```cpp
sort(
v.begin(),
v.end(),
less<int>()
);
```

作用：

升序排序。

---

# 3. plus()

## 用法

```cpp
plus<int>()
```

作用：

加法。

---

# 4. minus()

```cpp
minus<int>()
```

作用：

减法。

---

# 5. multiplies()

```cpp
multiplies<int>()
```

作用：

乘法。

---

# 6. divides()

```cpp
divides<int>()
```

作用：

除法。

---

# 六、lambda表达式（非常重要）

C++11以后：

可以直接写匿名函数。

---

# 1. 基本格式

```cpp
[捕获](参数)
{
    函数体
}
```

---

# 2. sort中使用（最常见）

例如：

按照升序：

```cpp
sort(
v.begin(),
v.end(),
[](int a,int b)
{
    return a<b;
}
);
```

等价：

```cpp
sort(v.begin(),v.end());
```

---

# 3. 降序

```cpp
sort(
v.begin(),
v.end(),
[](int a,int b)
{
    return a>b;
}
);
```

---

# 4. pair排序

按照second：

```cpp
sort(
v.begin(),
v.end(),
[](pair<int,int>a,pair<int,int>b)
{
    return a.second<b.second;
}
);
```

---

# 5. 自定义规则

例如：

奇数在前，偶数在后：

```cpp
sort(
v.begin(),
v.end(),
[](int a,int b)
{
    return a%2>b%2;
}
);
```

---

# 七、STL常见坑

---

# 1. vector删除元素

错误：

```cpp
for(auto x:v)
{
    if(x==3)
        v.erase(...);
}
```

原因：

删除导致迭代器失效。

---

正确：

```cpp
for(auto it=v.begin();
    it!=v.end();)
{
    if(*it==3)
        it=v.erase(it);

    else
        it++;
}
```

---

# 2. vector访问越界

错误：

```cpp
vector<int> v;

v[0]=10;
```

因为：

没有空间。

正确：

```cpp
v.push_back(10);
```

或者：

```cpp
vector<int> v(10);
```

---

# 3. map中[]自动创建

例如：

```cpp
map<string,int> mp;


cout<<mp["Tom"];
```

即使不存在：

也会创建：

```
Tom -> 0
```

如果只是查询：

用：

```cpp
find()
```

---

# 4. sort区间

错误：

```cpp
sort(v.begin(),v.end()-1);
```

少排序最后一个。

正确：

```cpp
sort(v.begin(),v.end());
```

---

# 5. unique必须配erase

错误：

```cpp
unique(v.begin(),v.end());
```

不会删除。

正确：

```cpp
v.erase(
unique(v.begin(),v.end()),
v.end()
);
```

---

# 6. lower_bound必须有序

错误：

```cpp
vector<int> v={5,1,3};

lower_bound(v.begin(),v.end(),3);
```

结果错误。

必须：

```cpp
sort(v.begin(),v.end());
```

---

# 7. priority_queue不能遍历

错误：

```cpp
for(auto x:pq)
```

priority_queue没有迭代器。

只能：

```cpp
while(!pq.empty())
{
    cout<<pq.top();

    pq.pop();
}
```

---

# 第九章 C++ STL函数速查表（机考打印版）

这一章按照**考试查阅习惯**整理。

格式：

> **名称 → 完整写法 → 参数怎么填 → 作用 → 示例**

---

# 一、vector速查表（重点）

头文件：

```cpp
#include <vector>
```

定义：

```cpp
vector<int> v;
```

---

# 1. push_back()

## 完整写法

```cpp
v.push_back(x);
```

## 参数

一个元素：

```cpp
x
```

例如：

```cpp
v.push_back(10);
```

## 作用

尾部添加元素。

---

# 2. pop_back()

## 完整写法

```cpp
v.pop_back();
```

## 参数

无。

## 作用

删除最后一个元素。

---

# 3. size()

## 完整写法

```cpp
v.size();
```

## 参数

无。

## 返回

元素个数。

例如：

```cpp
cout<<v.size();
```

---

# 4. empty()

## 完整写法

```cpp
v.empty();
```

## 参数

无。

## 返回

```cpp
true / false
```

作用：

判断是否为空。

---

# 5. clear()

## 完整写法

```cpp
v.clear();
```

参数：

无。

作用：

清空vector。

---

# 6. front()

## 完整写法

```cpp
v.front();
```

参数：

无。

作用：

访问第一个元素。

---

# 7. back()

## 完整写法

```cpp
v.back();
```

参数：

无。

作用：

访问最后元素。

---

# 8. begin()

## 完整写法

```cpp
v.begin();
```

参数：

无。

作用：

返回首迭代器。

---

# 9. end()

## 完整写法

```cpp
v.end();
```

参数：

无。

作用：

返回尾后迭代器。

---

# 10. insert()

## 完整写法

```cpp
v.insert(pos,value);
```

参数：

两个：

① 插入位置

② 插入值

例如：

```cpp
v.insert(v.begin()+2,100);
```

作用：

指定位置插入。

---

多个元素：

```cpp
v.insert(pos,n,value);
```

例如：

```cpp
v.insert(v.begin(),3,5);
```

结果：

```
5 5 5 ...
```

作用：

插入n个相同元素。

---

# 11. erase()

## 完整写法1

删除一个：

```cpp
v.erase(pos);
```

参数：

迭代器。

例如：

```cpp
v.erase(v.begin()+2);
```

作用：

删除指定位置。

---

## 完整写法2

删除区间：

```cpp
v.erase(first,last);
```

例如：

```cpp
v.erase(v.begin()+1,v.begin()+3);
```

删除：

下标1到2。

---

# 12. resize()

## 完整写法

```cpp
v.resize(n);
```

参数：

新大小。

例如：

```cpp
v.resize(10);
```

作用：

改变vector长度。

---

填充值：

```cpp
v.resize(10,0);
```

不足：

补0。

---

# 13. capacity()

## 完整写法

```cpp
v.capacity();
```

参数：

无。

作用：

返回当前容量。

注意：

不是元素数量。

区别：

```cpp
size()
```

实际元素数量。

```cpp
capacity()
```

申请空间大小。

---

# 14. reserve()

## 完整写法

```cpp
v.reserve(n);
```

参数：

预留容量。

例如：

```cpp
v.reserve(100);
```

作用：

提前申请空间。

减少扩容次数。

---

# 15. shrink_to_fit()

## 完整写法

```cpp
v.shrink_to_fit();
```

参数：

无。

作用：

尝试释放多余容量。

---

# 16. at()

## 完整写法

```cpp
v.at(index);
```

参数：

下标。

例如：

```cpp
v.at(2);
```

作用：

访问元素。

区别：

```cpp
v[2]
```

越界不检查。

```cpp
v.at(2)
```

越界抛异常。

---

# 二、string速查表（重点）

头文件：

```cpp
#include <string>
```

定义：

```cpp
string s;
```

---

# 1. size()

```cpp
s.size();
```

作用：

字符串长度。

---

# 2. length()

```cpp
s.length();
```

作用：

字符串长度。

和size完全一样。

---

# 3. empty()

```cpp
s.empty();
```

作用：

判断字符串为空。

---

# 4. clear()

```cpp
s.clear();
```

作用：

清空字符串。

---

# 5. push_back()

```cpp
s.push_back(c);
```

参数：

字符。

例如：

```cpp
s.push_back('a');
```

作用：

尾部添加字符。

---

# 6. pop_back()

```cpp
s.pop_back();
```

作用：

删除最后字符。

---

# 7. append()

## 写法

```cpp
s.append(str);
```

例如：

```cpp
s="hello";

s.append(" world");
```

结果：

```
hello world
```

作用：

追加字符串。

---

# 8. substr()

## 写法

```cpp
s.substr(pos,len);
```

两个参数：

① 起始位置

② 长度

例如：

```cpp
string s="abcdef";


cout<<s.substr(1,3);
```

结果：

```
bcd
```

作用：

截取子串。

---

# 9. find()

## 写法

```cpp
s.find(str);
```

参数：

要查找内容。

例如：

```cpp
s.find("abc");
```

返回：

第一次出现位置。

不存在：

```cpp
string::npos
```

---

# 10. replace()

## 写法

```cpp
s.replace(pos,len,str);
```

参数：

三个：

① 开始位置

② 替换长度

③ 新字符串

例如：

```cpp
s.replace(1,2,"xx");
```

作用：

替换字符串。

---

# 11. erase()

## 删除位置

```cpp
s.erase(pos,len);
```

例如：

```cpp
s.erase(1,3);
```

作用：

删除指定字符。

---

# 12. insert()

## 写法

```cpp
s.insert(pos,str);
```

例如：

```cpp
s.insert(2,"abc");
```

作用：

插入字符串。

---

# 13. compare()

## 写法

```cpp
s.compare(str);
```

作用：

比较字符串。

返回：

```cpp
0
```

相等。

---

# 14. getline()

## 写法

```cpp
getline(cin,s);
```

作用：

读取一整行。

区别：

```cpp
cin>>
```

遇空格停止。

getline：

包含空格。

---

# 三、algorithm速查表

头文件：

```cpp
#include <algorithm>
```

---

# 1. sort()

## 写法

```cpp
sort(begin,end);
```

参数：

两个迭代器。

例如：

```cpp
sort(v.begin(),v.end());
```

作用：

排序。

---

降序：

```cpp
sort(
v.begin(),
v.end(),
greater<int>()
);
```

---

# 2. reverse()

## 写法

```cpp
reverse(begin,end);
```

作用：

反转。

---

# 3. max()

## 写法

```cpp
max(a,b);
```

作用：

最大值。

---

# 4. min()

```cpp
min(a,b);
```

作用：

最小值。

---

# 5. max_element()

## 写法

```cpp
max_element(begin,end);
```

返回：

迭代器。

使用：

```cpp
*max_element(v.begin(),v.end());
```

作用：

最大元素。

---

# 6. min_element()

```cpp
*min_element(
v.begin(),
v.end()
);
```

作用：

最小元素。

---

# 7. find()

## 写法

```cpp
find(begin,end,x);
```

参数：

三个：

①开始

②结束

③查找值

作用：

寻找元素。

---

# 8. count()

```cpp
count(begin,end,x);
```

作用：

统计数量。

---

# 9. fill()

```cpp
fill(begin,end,x);
```

作用：

全部填充。

---

# 10. replace()

```cpp
replace(
begin,
end,
old,
new
);
```

作用：

替换元素。

---

# 11. unique()

```cpp
unique(begin,end);
```

作用：

删除连续重复。

常用：

```cpp
v.erase(
unique(v.begin(),v.end()),
v.end()
);
```

---

# 12. lower_bound()

```cpp
lower_bound(
begin,
end,
x
);
```

要求：

有序。

作用：

第一个：

```
>=x
```

---

# 13. upper_bound()

```cpp
upper_bound(
begin,
end,
x
);
```

作用：

第一个：

```
>x
```

---

# 14. binary_search()

```cpp
binary_search(
begin,
end,
x
);
```

作用：

二分查找是否存在。

返回：

true/false。

---

# 15. next_permutation()

```cpp
next_permutation(
begin,
end
);
```

作用：

下一个排列。

---

# 四、numeric速查表

头文件：

```cpp
#include <numeric>
```

---

# 1. accumulate()

## 写法

```cpp
accumulate(
begin,
end,
init
);
```

参数：

三个：

①开始

②结束

③初始值

例如：

```cpp
accumulate(
v.begin(),
v.end(),
0
);
```

作用：

求和。

---

# 2. iota()

```cpp
iota(
begin,
end,
start
);
```

作用：

连续赋值。

例如：

```cpp
iota(v.begin(),v.end(),1);
```

结果：

```
1 2 3...
```

---

# 3. partial_sum()

```cpp
partial_sum(
begin,
end,
out
);
```

作用：

前缀和。

---

# 五、map速查表

定义：

```cpp
map<int,int> mp;
```

---

# 1. []

```cpp
mp[key];
```

作用：

访问value。

不存在自动创建。

---

# 2. insert()

```cpp
mp.insert({key,value});
```

作用：

插入。

---

# 3. erase()

```cpp
mp.erase(key);
```

作用：

删除。

---

# 4. find()

```cpp
mp.find(key);
```

作用：

查找key。

---

# 5. count()

```cpp
mp.count(key);
```

作用：

判断key是否存在。

---

# 6. 遍历

```cpp
for(auto p:mp)
{
    cout<<p.first;
    cout<<p.second;
}
```

---

# 六、stack速查表

定义：

```cpp
stack<int> st;
```

---

push：

```cpp
st.push(x);
```

入栈。

---

pop：

```cpp
st.pop();
```

删除栈顶。

---

top：

```cpp
st.top();
```

访问栈顶。

---

empty：

```cpp
st.empty();
```

判断空。

---

# 七、queue速查表

定义：

```cpp
queue<int> q;
```

---

push：

```cpp
q.push(x);
```

入队。

---

pop：

```cpp
q.pop();
```

出队。

---

front：

```cpp
q.front();
```

队头。

---

back：

```cpp
q.back();
```

队尾。

---

# 八、priority_queue速查表

定义：

```cpp
priority_queue<int> pq;
```

---

push：

```cpp
pq.push(x);
```

---

top：

```cpp
pq.top();
```

作用：

最大值。

---

pop：

```cpp
pq.pop();
```

---

小根堆：

```cpp
priority_queue<
int,
vector<int>,
greater<int>
> pq;
```

---

# 九、set速查表

定义：

```cpp
set<int> s;
```

---

插入：

```cpp
s.insert(x);
```

删除：

```cpp
s.erase(x);
```

查找：

```cpp
s.find(x);
```

数量：

```cpp
s.size();
```

判断存在：

```cpp
s.count(x);
```

---

# 十、考试最常忘参数总结

| 函数            | 参数                 |
| --------------- | -------------------- |
| sort            | 开始，结束，比较函数 |
| reverse         | 开始，结束           |
| find            | 开始，结束，值       |
| count           | 开始，结束，值       |
| fill            | 开始，结束，值       |
| accumulate      | 开始，结束，初始值   |
| lower_bound     | 开始，结束，值       |
| upper_bound     | 开始，结束，值       |
| substr          | 位置，长度           |
| erase(vector)   | 位置 或 区间         |
| insert(vector)  | 位置，值             |
| replace(string) | 位置，长度，新字符串 |
| resize          | 新大小               |
| reserve         | 容量                 |

---

# 第十章 C++ 常用头文件速查（cmath + 其他高频库）

这一部分整理**上机考试除了 STL 之外最常用的函数库**。

重点：

- `<cmath>` 数学函数（重点）
- `<iomanip>` 格式化输出
- `<cstdlib>` 常用工具函数
- `<cstring>` C字符串处理
- `<ctime>` 时间函数
- `<numeric>` 数学运算
- `<sstream>` 字符串流
- `<fstream>` 文件操作（了解）
- `<random>` 随机数
- `<limits>` 极值

---

# 一、cmath（数学函数库）⭐⭐⭐⭐⭐

头文件：

```cpp
#include <cmath>
```

作用：

提供数学计算函数。

---

# 1. abs()

## 名称

```cpp
abs()
```

---

## 用法

```cpp
abs(x);
```

参数：

一个数字。

例如：

```cpp
int x=-10;

cout<<abs(x);
```

输出：

```
10
```

---

## 作用

求绝对值。

注意：

整数：

```cpp
abs()
```

浮点：

推荐：

```cpp
fabs()
```

---

# 2. fabs()

## 名称

```cpp
fabs()
```

---

## 用法

```cpp
fabs(x);
```

例如：

```cpp
double x=-3.14;

cout<<fabs(x);
```

输出：

```
3.14
```

---

## 作用

求浮点数绝对值。

---

# 3. sqrt()

## 名称

```cpp
sqrt()
```

---

## 用法

```cpp
sqrt(x);
```

例如：

```cpp
cout<<sqrt(9);
```

输出：

```
3
```

浮点：

```cpp
cout<<sqrt(2.0);
```

输出：

```
1.414...
```

---

## 作用

平方根。

---

# 4. pow()

## 名称

```cpp
pow()
```

---

## 用法

两个参数：

```cpp
pow(a,b);
```

表示：

```
a的b次方
```

例如：

```cpp
cout<<pow(2,3);
```

输出：

```
8
```

---

## 作用

幂运算。

常见：

平方：

```cpp
pow(x,2)
```

立方：

```cpp
pow(x,3)
```

---

# 5. exp()

## 名称

```cpp
exp()
```

---

## 用法

```cpp
exp(x);
```

表示：

[
e^x
]

例如：

```cpp
cout<<exp(1);
```

约：

```
2.71828
```

---

## 作用

计算自然指数。

---

# 6. log()

## 名称

```cpp
log()
```

---

## 用法

```cpp
log(x);
```

表示：

[
ln(x)
]

例如：

```cpp
cout<<log(2);
```

---

## 作用

自然对数。

---

# 7. log10()

## 名称

```cpp
log10()
```

---

## 用法

```cpp
log10(x);
```

例如：

```cpp
cout<<log10(100);
```

输出：

```
2
```

---

## 作用

十进制对数。

---

# 8. sin()

## 名称

```cpp
sin()
```

---

## 用法

```cpp
sin(x);
```

注意：

参数单位：

**弧度**

不是角度。

例如：

```cpp
sin(M_PI/2);
```

结果：

```
1
```

---

## 作用

正弦。

---

# 9. cos()

## 用法

```cpp
cos(x);
```

作用：

余弦。

---

# 10. tan()

## 用法

```cpp
tan(x);
```

作用：

正切。

---

# 11. asin()

## 名称

```cpp
asin()
```

作用：

反正弦。

返回：

弧度。

---

# 12. acos()

```cpp
acos(x);
```

作用：

反余弦。

---

# 13. atan()

```cpp
atan(x);
```

作用：

反正切。

---

# 14. atan2()

⭐⭐重要

## 用法

```cpp
atan2(y,x);
```

返回：

点：

[
(x,y)
]

的角度。

例如：

```cpp
double angle=atan2(y,x);
```

---

## 作用

计算方向角。

常用于：

- 几何
- 向量

---

# 15. ceil()

## 名称

```cpp
ceil()
```

---

## 用法

```cpp
ceil(x);
```

例如：

```cpp
ceil(3.2);
```

结果：

```
4
```

---

## 作用

向上取整。

---

# 16. floor()

## 名称

```cpp
floor()
```

---

## 用法

```cpp
floor(x);
```

例如：

```cpp
floor(3.8);
```

结果：

```
3
```

---

## 作用

向下取整。

---

# 17. round()

## 名称

```cpp
round()
```

---

## 用法

```cpp
round(x);
```

例如：

```cpp
round(3.5);
```

结果：

```
4
```

---

## 作用

四舍五入。

---

# 18. trunc()

## 名称

```cpp
trunc()
```

---

## 用法

```cpp
trunc(x);
```

例如：

```cpp
trunc(3.8);
```

结果：

```
3
```

---

## 作用

截断小数。

---

# 19. fmod()

## 名称

```cpp
fmod()
```

---

## 用法

```cpp
fmod(a,b);
```

例如：

```cpp
fmod(5.5,2);
```

结果：

```
1.5
```

---

## 作用

浮点取余。

类似：

整数：

```cpp
a%b
```

浮点：

```cpp
fmod(a,b)
```

---

# 20. hypot()

## 名称

```cpp
hypot()
```

---

## 用法

```cpp
hypot(a,b);
```

计算：

[
\sqrt{a^2+b^2}
]

例如：

```cpp
hypot(3,4);
```

结果：

```
5
```

---

## 作用

求直角三角形斜边。

---

# cmath常量

## M_PI

圆周率：

```cpp
M_PI
```

例如：

```cpp
double r=2;

double area=M_PI*r*r;
```

---

# 二、iomanip（格式化输出）⭐⭐⭐⭐

头文件：

```cpp
#include <iomanip>
```

---

# 1. setprecision()

## 名称

```cpp
setprecision()
```

---

## 用法

```cpp
cout<<setprecision(n);
```

例如：

```cpp
cout<<setprecision(3)<<3.14159;
```

输出：

```
3.14
```

---

默认：

控制有效数字。

---

配合：

```cpp
fixed
```

---

# 2. fixed

## 用法

```cpp
cout<<fixed;
```

作用：

固定小数位。

---

# 3. setprecision + fixed

⭐⭐⭐⭐⭐

```cpp
cout
<<fixed
<<setprecision(2)
<<3.14159;
```

输出：

```
3.14
```

考试最常用。

---

# 4. setw()

## 名称

```cpp
setw()
```

---

## 用法

```cpp
setw(n)
```

例如：

```cpp
cout<<setw(5)<<123;
```

输出：

```
  123
```

---

## 作用

设置输出宽度。

---

# 5. left

```cpp
cout<<left;
```

作用：

左对齐。

---

# 6. right

```cpp
cout<<right;
```

作用：

右对齐。

---

# 7. setfill()

## 用法

```cpp
setfill('*')
```

例如：

```cpp
cout
<<setfill('0')
<<setw(4)
<<12;
```

输出：

```
0012
```

---

# 三、cstring（C风格字符串）

头文件：

```cpp
#include <cstring>
```

处理：

```cpp
char数组
```

---

# 1. strlen()

## 名称

```cpp
strlen()
```

## 用法

```cpp
strlen(str);
```

例如：

```cpp
char s[]="hello";


cout<<strlen(s);
```

输出：

```
5
```

---

## 作用

计算字符串长度。

---

# 2. strcpy()

## 用法

```cpp
strcpy(dest,src);
```

作用：

复制字符串。

---

# 3. strcat()

## 用法

```cpp
strcat(a,b);
```

作用：

连接字符串。

---

# 4. strcmp()

## 用法

```cpp
strcmp(a,b);
```

作用：

比较字符串。

返回：

```
0 相等
<0 小于
>0 大于
```

---

# 5. memset()

⭐⭐⭐⭐⭐

## 用法

```cpp
memset(数组,值,sizeof(数组));
```

例如：

```cpp
int a[100];


memset(a,0,sizeof(a));
```

作用：

快速初始化数组。

---

常用：

全部变：

0：

```cpp
memset(a,0,sizeof(a));
```

-1：

```cpp
memset(a,-1,sizeof(a));
```

---

注意：

不要：

```cpp
memset(a,1,sizeof(a));
```

整数不会全部变1。

---

# 四、cstdlib（常用工具）

头文件：

```cpp
#include <cstdlib>
```

---

# 1. rand()

## 用法

```cpp
rand();
```

作用：

生成随机数。

---

# 2. srand()

## 用法

```cpp
srand(time(0));
```

作用：

设置随机种子。

通常：

```cpp
srand(time(NULL));
```

---

# 3. atoi()

## 用法

```cpp
atoi(str);
```

例如：

```cpp
char s[]="123";


int x=atoi(s);
```

作用：

字符串转整数。

---

# 4. exit()

## 用法

```cpp
exit(0);
```

作用：

结束程序。

---

# 五、sstream（字符串流）

头文件：

```cpp
#include <sstream>
```

---

# 1. stringstream

## 定义

```cpp
stringstream ss;
```

---

# 字符串转数字

```cpp
string s="123";


stringstream ss(s);


int x;


ss>>x;
```

结果：

```
x=123
```

---

# 数字转字符串

```cpp
stringstream ss;


ss<<123;


string s;


ss>>s;
```

---

## 作用

字符串和数字互转。

---

# 六、limits（极值）

头文件：

```cpp
#include <limits>
```

---

# 1. INT_MAX

## 用法

```cpp
INT_MAX
```

作用：

int最大值。

约：

```
2147483647
```

---

# 2. INT_MIN

```cpp
INT_MIN
```

作用：

int最小值。

---

# 3. numeric_limits

## 用法

```cpp
numeric_limits<int>::max();
```

作用：

获取类型最大值。

例如：

```cpp
long long INF=
numeric_limits<long long>::max();
```

---

# 七、常用头文件总表（考试版）

| 头文件    | 作用       | 常用               |
| --------- | ---------- | ------------------ |
| cmath     | 数学计算   | sqrt,pow,sin       |
| iomanip   | 格式输出   | fixed,setprecision |
| string    | 字符串     | string             |
| cstring   | C字符串    | strlen,memset      |
| sstream   | 字符串转换 | stringstream       |
| cstdlib   | 工具函数   | rand,exit          |
| ctime     | 时间       | time               |
| limits    | 极值       | INT_MAX            |
| numeric   | 数学算法   | accumulate,iota    |
| algorithm | 算法       | sort,find          |
| vector    | 动态数组   | vector             |
| queue     | 队列       | queue              |
| stack     | 栈         | stack              |
| map       | 映射       | map                |
| set       | 集合       | set                |

---

# 第十一章 C++ 常用语法速查（机考必备）

这一章不是 STL，而是**写题时每天都会用到的 C++ 基础语法工具**。

重点：

- 输入输出
- 类型转换
- 字符处理
- ASCII
- 位运算
- long long
- 常用技巧
- 防止错误

---

# 一、输入输出（iostream）

头文件：

```cpp
#include <iostream>
```

---

# 1. cin 输入

## 用法

```cpp
cin >> x;
```

例如：

```cpp
int a;

cin >> a;
```

作用：

读取变量。

---

# 2. 多个变量输入

```cpp
int a,b,c;

cin>>a>>b>>c;
```

输入：

```
1 2 3
```

结果：

```
a=1,b=2,c=3
```

---

# 3. cout 输出

## 用法

```cpp
cout << x;
```

例如：

```cpp
cout<<123;
```

输出：

```
123
```

---

# 4. 输出多个内容

```cpp
cout<<"sum="<<sum;
```

---

# 5. 换行

## 方法1

```cpp
cout<<endl;
```

作用：

换行，并刷新缓冲区。

---

## 方法2（推荐）

```cpp
cout<<"\n";
```

速度更快。

考试推荐：

```cpp
cout<<"\n";
```

---

# 6. getline()

⭐⭐⭐⭐⭐

## 用法

```cpp
string s;

getline(cin,s);
```

输入：

```
hello world
```

读取：

```
hello world
```

---

## 注意

如果前面用了：

```cpp
cin>>n;
```

后面：

```cpp
getline(cin,s);
```

可能读到空行。

解决：

```cpp
cin.ignore();
```

例如：

```cpp
int n;

cin>>n;

cin.ignore();

getline(cin,s);
```

---

# 二、类型转换（非常重要）

---

# 1. int 转 double

自动转换：

```cpp
int a=5;

double b=a;
```

结果：

```
5.0
```

---

# 2. 强制类型转换

## 写法1

```cpp
(double)a
```

例如：

```cpp
int a=5,b=2;


cout<<(double)a/b;
```

结果：

```
2.5
```

---

## 写法2（C++推荐）

```cpp
static_cast<double>(a)
```

例如：

```cpp
double x=
static_cast<double>(a)/b;
```

---

# 3. string 转 int

## stoi()

头文件：

```cpp
#include <string>
```

用法：

```cpp
string s="123";


int x=stoi(s);
```

结果：

```
123
```

---

# 4. string 转 long long

```cpp
long long x=stoll(s);
```

---

# 5. string 转 double

```cpp
double x=stod(s);
```

---

# 6. int 转 string

## to_string()

用法：

```cpp
string s=to_string(123);
```

结果：

```
"123"
```

---

# 三、字符处理（cctype）

头文件：

```cpp
#include <cctype>
```

---

# 1. isdigit()

## 名称

判断数字字符

## 用法

```cpp
isdigit(c);
```

例如：

```cpp
char c='5';


if(isdigit(c))
{
    cout<<"数字";
}
```

作用：

判断：

```
0~9
```

---

# 2. isalpha()

判断字母。

```cpp
isalpha(c);
```

例如：

```cpp
'a'
```

返回：

true。

---

# 3. islower()

判断小写。

```cpp
islower(c);
```

---

# 4. isupper()

判断大写。

```cpp
isupper(c);
```

---

# 5. tolower()

转换小写。

```cpp
tolower(c);
```

例如：

```cpp
char c='A';

c=tolower(c);
```

结果：

```
a
```

---

# 6. toupper()

转换大写。

```cpp
toupper(c);
```

例如：

```
a→A
```

---

# 四、ASCII码（高频）

字符本质：

数字。

---

# 1. 字符转数字

例如：

```cpp
char c='5';


int x=c-'0';
```

结果：

```
5
```

原理：

ASCII：

```
'5'=53

'0'=48

53-48=5
```

---

# 2. 数字转字符

```cpp
char c=x+'0';
```

例如：

```cpp
int x=7;

char c=x+'0';
```

结果：

```
'7'
```

---

# 3. 字母大小写转换

大写：

```cpp
char c='A';

c=c+32;
```

得到：

```
a
```

---

小写转大写：

```cpp
c=c-32;
```

不过推荐：

```cpp
toupper()
tolower()
```

---

# 五、long long使用（非常重要）

---

# 1. int范围

```text
-2^31 ~ 2^31-1
```

约：

```
21亿
```

---

# 2. long long范围

约：

```
9×10^18
```

---

# 3. 定义

```cpp
long long x;
```

---

# 4. 常量

错误：

```cpp
long long x=1000000000000;
```

可能溢出。

正确：

```cpp
long long x=1000000000000LL;
```

---

# 六、三目运算符

## 名称

条件运算符

---

## 格式

```cpp
条件 ? 真 : 假;
```

---

例如：

```cpp
int max=
a>b?a:b;
```

等价：

```cpp
if(a>b)
max=a;

else
max=b;
```

---

# 七、位运算（了解）

符号：

```
&
|
^
~
<<
>>
```

---

# 1. 按位与 &

例如：

```cpp
5&3
```

二进制：

```
101
011
---
001
```

结果：

```
1
```

作用：

判断奇偶。

---

# 判断奇偶⭐⭐⭐

偶数：

```cpp
if((x&1)==0)
```

奇数：

```cpp
if(x&1)
```

---

# 2. 按位或 |

```cpp
a|b
```

作用：

合并二进制位。

---

# 3. 异或 ^

## 特点

相同：

```
0
```

不同：

```
1
```

---

应用：

交换变量：

```cpp
a^=b;

b^=a;

a^=b;
```

---

# 4. 左移 <<

```cpp
x<<1
```

相当：

```
x*2
```

例如：

```cpp
5<<1
```

结果：

```
10
```

---

# 5. 右移 >>

```cpp
x>>1
```

相当：

```
x/2
```

---

# 八、常用数学技巧

---

# 1. 最大公约数 gcd

头文件：

```cpp
#include <numeric>
```

用法：

```cpp
gcd(a,b);
```

例如：

```cpp
cout<<gcd(12,8);
```

输出：

```
4
```

---

# 2. 最小公倍数 lcm

C++17：

```cpp
lcm(a,b);
```

例如：

```cpp
cout<<lcm(4,6);
```

输出：

```
12
```

---

# 3. 判断素数模板

```cpp
bool isPrime(int n)
{
    if(n<2)
        return false;


    for(int i=2;i*i<=n;i++)
    {
        if(n%i==0)
            return false;
    }


    return true;
}
```

---

# 九、swap交换

## 名称

```cpp
swap()
```

头文件：

```cpp
#include <algorithm>
```

---

## 用法

```cpp
swap(a,b);
```

例如：

```cpp
int a=1,b=2;

swap(a,b);
```

结果：

```
a=2,b=1
```

---

# 十、auto自动类型推导

---

## 用法

```cpp
auto x=10;
```

等价：

```cpp
int x=10;
```

---

vector遍历：

```cpp
for(auto x:v)
{
}
```

map：

```cpp
for(auto p:mp)
{
}
```

---

# 十一、引用 &（STL常用）

---

## 普通变量

```cpp
int a=10;


int &b=a;
```

b就是a别名。

---

## 修改vector元素

错误：

```cpp
for(auto x:v)
{
    x++;
}
```

不会改变。

---

正确：

```cpp
for(auto &x:v)
{
    x++;
}
```

---

# 十二、const（防止修改）

---

定义：

```cpp
const int x=10;
```

不能：

```cpp
x=20;
```

---

常见：

```cpp
const vector<int>& v
```

表示：

引用vector，但不能修改。

---

# 十三、宏定义（了解）

---

# 1. 常量

```cpp
#define PI 3.14159
```

---

# 2. 宏函数

```cpp
#define MAX(a,b) ((a)>(b)?(a):(b))
```

现在C++更推荐：

```cpp
const
inline
```

---

# 十四、结构体 struct

考试经常用。

---

定义：

```cpp
struct Student
{
    int id;

    string name;

};
```

---

使用：

```cpp
Student s;


s.id=1;

s.name="Tom";
```

---

数组：

```cpp
Student a[100];
```

---

vector：

```cpp
vector<Student> v;
```

---

# 十五、自定义排序（结构体）

例如：

```cpp
struct Node
{
    int x;

    int y;
};


bool cmp(Node a,Node b)
{
    return a.x<b.x;
}
```

使用：

```cpp
sort(v.begin(),v.end(),cmp);
```

---

# 十六、万能main模板（考试推荐）

```cpp
#include <bits/stdc++.h>

using namespace std;


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);




    return 0;
}
```

---

# 十七、考试最常用组合

## 1. 输入数组排序求和

```cpp
vector<int> v(n);


for(auto &x:v)
    cin>>x;


sort(v.begin(),v.end());


int sum=
accumulate(v.begin(),v.end(),0);
```

---

## 2. 统计字符次数

```cpp
map<char,int> mp;


for(char c:s)
{
    mp[c]++;
}
```

---

## 3. 删除重复元素

```cpp
sort(v.begin(),v.end());


v.erase(
unique(v.begin(),v.end()),
v.end()
);
```

---

## 4. 保留两位小数

```cpp
cout
<<fixed
<<setprecision(2)
<<x;
```

---

## 5. 最大最小值

```cpp
int mx=*max_element(
v.begin(),
v.end()
);


int mn=*min_element(
v.begin(),
v.end()
);
```

---

# 最终考试头文件优先级

⭐⭐⭐⭐⭐ 必背：

```cpp
#include <bits/stdc++.h>
```

里面全部包含。

重点掌握：

```
vector
string
algorithm
numeric
cmath
iomanip
map
set
queue
stack
sstream
cstring
```

---

# 第十二章 STL 自定义排序手册

## STL

### └── algorithm

### 　　└── sort

### 　　　　├── cmp函数

### 　　　　├── lambda表达式

### 　　　　├── 结构体排序

### 　　　　└── 多关键字排序

---

# 一、sort 基础

## 1. 头文件

```cpp
#include <algorithm>
```

---

## 2. 基本用法

```cpp
sort(开始位置, 结束位置);
```

默认：

- 升序排序
- 使用 `<` 比较

例如：

```cpp
vector<int> a = {5,3,1,4,2};

sort(a.begin(), a.end());
```

结果：

```
1 2 3 4 5
```

---

## 3. 降序排序

使用第三个参数：

```cpp
sort(a.begin(), a.end(), greater<int>());
```

结果：

```
5 4 3 2 1
```

需要：

```cpp
#include <functional>
```

---

# 二、cmp 函数（自定义排序函数）

## 1. cmp 是什么？

cmp：

> compare（比较）的缩写

作用：

告诉 `sort`

**两个元素谁应该排在前面**

---

sort内部逻辑：

```cpp
cmp(a,b)
```

如果返回：

```cpp
true
```

表示：

```
a应该排在b前面
```

如果返回：

```cpp
false
```

表示：

```
a不应该排在b前面
```

---

## 2. cmp 函数基本格式

```cpp
bool cmp(数据类型 a, 数据类型 b)
{
    return 排序规则;
}
```

然后：

```cpp
sort(begin,end,cmp);
```

---

# 三、cmp 函数给 int 排序

## 1. 升序

默认情况：

```cpp
bool cmp(int a,int b)
{
    return a<b;
}
```

例如：

```cpp
sort(v.begin(),v.end(),cmp);
```

结果：

```
1 2 3 4 5
```

---

## 2. 降序

```cpp
bool cmp(int a,int b)
{
    return a>b;
}
```

结果：

```
5 4 3 2 1
```

---

# 四、cmp 函数给字符串排序

## 1. 按字典序升序

```cpp
bool cmp(string a,string b)
{
    return a<b;
}
```

---

## 2. 按长度排序

```cpp
bool cmp(string a,string b)
{
    return a.size()<b.size();
}
```

例如：

输入：

```
apple
a
banana
```

结果：

```
a
apple
banana
```

---

# 五、lambda 表达式排序

## 1. lambda 是什么？

简单理解：

> 临时写一个函数，不需要名字

普通函数：

```cpp
bool cmp(int a,int b)
{
    return a>b;
}
```

lambda：

```cpp
[](int a,int b)
{
    return a>b;
}
```

---

## 2. lambda 基本格式

```cpp
[](参数)
{
    函数体
}
```

完整：

```cpp
sort(v.begin(),v.end(),
    [](int a,int b)
    {
        return a>b;
    }
);
```

作用：

降序排列。

---

## 3. lambda 各部分含义

例如：

```cpp
[](int a,int b)
{
    return a<b;
}
```

## [] 捕获列表

控制外部变量使用。

常用：

### 不使用外部变量

```cpp
[]
```

最常见。

---

### 使用外部变量

```cpp
[&]
```

引用捕获。

例如：

```cpp
int x=10;

sort(v.begin(),v.end(),
[&](int a,int b)
{
    return a+x<b+x;
});
```

---

## 参数

```cpp
(int a,int b)
```

表示比较两个元素。

---

## 返回值

```cpp
return a<b;
```

表示排序规则。

---

# 六、cmp 与 lambda 对比

## cmp函数

```cpp
bool cmp(int a,int b)
{
    return a>b;
}


sort(v.begin(),v.end(),cmp);
```

优点：

- 清晰
- 可以重复使用

---

## lambda

```cpp
sort(v.begin(),v.end(),
[](int a,int b)
{
    return a>b;
});
```

优点：

- 简洁
- 一次性使用方便

考试推荐：

小规则：

```cpp
lambda
```

复杂规则：

```cpp
cmp函数
```

---

# 七、结构体排序

## 1. 为什么需要结构体排序？

例如：

学生：

```cpp
姓名
成绩
年龄
```

不能直接：

```cpp
sort(student.begin(),student.end());
```

需要告诉sort：

按照什么排。

---

## 2. 定义结构体

```cpp
struct Student
{
    string name;
    int score;
    int age;
};
```

---

存储：

```cpp
vector<Student> s;
```

---

## 3. 结构体 cmp 排序

格式：

```cpp
bool cmp(Student a,Student b)
{
    return a.score>b.score;
}
```

表示：

成绩高的排前面。

使用：

```cpp
sort(s.begin(),s.end(),cmp);
```

---

## 4. 结构体 + lambda

也可以：

```cpp
sort(s.begin(),s.end(),
[](Student a,Student b)
{
    return a.score>b.score;
});
```

---

# 八、多关键字排序（重点）

实际考试最常见。

例如：

学生排序规则：

1. 总分高优先
2. 数学高优先
3. 英语高优先
4. 输入早优先

---

结构体：

```cpp
struct Student
{
    string name;

    int sum;
    int math;
    int english;

    int id;
};
```

其中：

```cpp
id
```

表示输入顺序。

---

## 1. 多关键字 cmp 写法

### 写法一：if 判断（推荐）

```cpp
bool cmp(Student a,Student b)
{
    if(a.sum!=b.sum)
        return a.sum>b.sum;

    if(a.math!=b.math)
        return a.math>b.math;

    if(a.english!=b.english)
        return a.english>b.english;

    return a.id<b.id;
}
```

---

解释：

第一关键字：

```cpp
sum
```

不同：

直接决定顺序。

相同：

比较第二关键字：

```cpp
math
```

还相同：

比较第三关键字：

```cpp
english
```

全部相同：

按照输入顺序：

```cpp
id小的在前
```

---

## 2. 多关键字 lambda

```cpp
sort(v.begin(),v.end(),
[](Student a,Student b)
{
    if(a.sum!=b.sum)
        return a.sum>b.sum;

    if(a.math!=b.math)
        return a.math>b.math;

    return a.id<b.id;
});
```

---

# 九、pair 排序

STL中：

```cpp
pair
```

默认：

先比较first

再比较second

例如：

```cpp
vector<pair<int,int>> v;
```

排序：

```cpp
sort(v.begin(),v.end());
```

等价：

```cpp
if(first不同)
    first小的优先

else
    second小的优先
```

---

## 1. pair 自定义排序

例如：

第二个元素降序。

```cpp
sort(v.begin(),v.end(),
[](pair<int,int>a,pair<int,int>b)
{
    return a.second>b.second;
});
```

---

# 十、sort 注意事项

## 1. cmp 必须返回 bool

正确：

```cpp
return a<b;
```

错误：

```cpp
return a-b;
```

---

## 2. 不能写等号

错误：

```cpp
return a<=b;
```

原因：

sort要求严格弱排序。

正确：

```cpp
return a<b;
```

---

## 3. 参数最好加引用

推荐：

```cpp
bool cmp(Student &a,Student &b)
```

或者：

```cpp
bool cmp(const Student &a,const Student &b)
```

避免复制。

考试常用：

```cpp
bool cmp(const Student&a,const Student&b)
```

---

# 十一、考试万能模板

## int排序

```cpp
sort(v.begin(),v.end(),
[](int a,int b)
{
    return a>b;
});
```

---

## 结构体排序

```cpp
struct Node
{
    int a;
    int b;
};


bool cmp(const Node&a,const Node&b)
{
    if(a.a!=b.a)
        return a.a>b.a;

    return a.b<b.b;
}


sort(v.begin(),v.end(),cmp);
```

---

## 多关键字排序万能模板

```cpp
bool cmp(const Node&a,const Node&b)
{
    if(a.x!=b.x)
        return a.x>b.x;

    if(a.y!=b.y)
        return a.y<b.y;

    return a.id<b.id;
}
```

---

# 十二、总结

| 内容         | 作用           |
| ------------ | -------------- |
| sort         | 排序函数       |
| cmp          | 告诉sort谁在前 |
| lambda       | 临时cmp        |
| struct排序   | 给对象排序     |
| 多关键字排序 | if逐层判断     |
| return true  | a排前面        |
| return false | a排后面        |

---

**考试重点优先级：**

```
★★★★★ 多关键字结构体排序
★★★★★ cmp函数
★★★★★ lambda表达式
★★★★ struct定义
★★ pair排序
★★ greater降序
```

掌握这部分，基本覆盖 C++ 上机考试里 90% 的自定义排序题。

# 第十三章 C++ 进制转换知识点总结（考场速查版）

---

# 一、进制基础知识

## 1. 什么是进制

**进制：用多少个数字表示一个数。**

例如：

| 进制                 | 数字范围 | 常见表示               |
| -------------------- | -------- | ---------------------- |
| 二进制 Binary        | 0~1      | 计算机底层             |
| 八进制 Octal         | 0~7      | 较少使用               |
| 十进制 Decimal       | 0~9      | 日常使用               |
| 十六进制 Hexadecimal | 0~9+A~F  | 内存地址、颜色、机器码 |

---

## 2. 位权表示法

一个数：

[
(abc)_n
]

表示：

[
a\times n^2+b\times n^1+c\times n^0
]

例如：

二进制：

[
1011_2
]

转换十进制：

[
1\times2^3+0\times2^2+1\times2^1+1\times2^0
]

=

[
8+0+2+1=11
]

所以：

```
1011(二进制)=11(十进制)
```

---

# 二、十进制转换其他进制（重点）

## 方法：除基取余法

### 原理

不断：

```
目标进制
↑
除以基数
↑
记录余数
```

最后：

**余数倒序排列**

---

## 1. 十进制 → 二进制

例如：

```
13 转二进制
```

过程：

```
13 ÷ 2 = 6 余1
6  ÷ 2 = 3 余0
3  ÷ 2 = 1 余1
1  ÷ 2 = 0 余1
```

倒序：

```
1101
```

所以：

```
13 = 1101(2)
```

---

## C++实现

### 方法1：while循环

```cpp
int n;

cin >> n;

string s;

while(n)
{
    s += char(n % 2 + '0');
    n /= 2;
}

reverse(s.begin(),s.end());

cout << s;
```

---

## 通用进制转换模板

十进制转任意进制：

```cpp
string convert(int n,int base)
{
    string ans;

    while(n)
    {
        int t=n%base;

        if(t<10)
            ans+=char(t+'0');
        else
            ans+=char(t-10+'A');

        n/=base;
    }

    reverse(ans.begin(),ans.end());

    return ans;
}
```

支持：

```
2进制
8进制
16进制
```

例如：

```cpp
cout<<convert(255,16);
```

输出：

```
FF
```

---

# 三、其他进制 → 十进制

## 方法：按权展开

例如：

```
1011(2)
```

计算：

```
1*2^3
+
0*2^2
+
1*2^1
+
1*2^0
```

---

## C++实现

### 方法1：循环累加

公式：

[
ans=ans\times base+当前数字
]

例如：

```
1011
```

过程：

```
ans=0

1:
ans=0*2+1=1

0:
ans=1*2+0=2

1:
ans=2*2+1=5

1:
ans=5*2+1=11
```

---

代码：

```cpp
int toDecimal(string s,int base)
{
    int ans=0;

    for(char c:s)
    {
        int x;

        if(c>='0'&&c<='9')
            x=c-'0';
        else
            x=c-'A'+10;


        ans=ans*base+x;
    }

    return ans;
}
```

例如：

```cpp
cout<<toDecimal("FF",16);
```

输出：

```
255
```

---

# 四、二进制、八进制、十六进制互转

---

# 1. 二进制 ↔ 八进制

因为：

[
8=2^3
]

所以：

**三位二进制对应一位八进制**

---

例：

```
110101
```

三位分组：

```
110 101
```

转换：

```
110=6
101=5
```

所以：

```
110101(2)=65(8)
```

---

# 2. 二进制 ↔ 十六进制

因为：

[
16=2^4
]

**四位二进制对应一位十六进制**

例如：

```
11111111
```

分组：

```
1111 1111
```

对应：

```
F F
```

结果：

```
FF
```

---

# 五、C++内置进制转换函数（重点）

头文件：

```cpp
#include <iostream>
#include <bitset>
#include <string>
```

---

# 1. bitset（二进制转换神器）

## 定义

```cpp
bitset<长度>
```

表示固定长度二进制。

---

## 十进制 → 二进制

```cpp
int n=10;

bitset<8> b(n);

cout<<b;
```

输出：

```
00001010
```

---

## 获取字符串

```cpp
string s=b.to_string();
```

---

## 二进制 → 十进制

```cpp
bitset<8> b("1010");

cout<<b.to_ulong();
```

输出：

```
10
```

---

# bitset常用函数

| 函数        | 作用            |
| ----------- | --------------- |
| count()     | 统计1数量       |
| size()      | 长度            |
| to_string() | 转字符串        |
| to_ulong()  | 转unsigned long |
| reset()     | 全部置0         |
| set()       | 全部置1         |
| flip()      | 取反            |

---

例：

```cpp
bitset<8>b("1010");

cout<<b.count();
```

输出：

```
2
```

---

# 六、C++字符串进制转换函数

---

## 1. stoi()

字符串 → 十进制整数

格式：

```cpp
stoi(string, nullptr, base);
```

---

例：

二进制：

```cpp
string s="1010";

int x=stoi(s,nullptr,2);

cout<<x;
```

输出：

```
10
```

---

十六进制：

```cpp
int x=stoi("FF",nullptr,16);
```

结果：

```
255
```

---

支持：

```
base=2
base=8
base=10
base=16
```

---

# 2. stoi相关函数

| 函数  | 作用             |
| ----- | ---------------- |
| stoi  | string→int       |
| stol  | string→long      |
| stoll | string→long long |
| stod  | string→double    |

---

# 七、C++输出不同进制

头文件：

```cpp
#include<iomanip>
```

---

## 十进制

默认：

```cpp
cout<<100;
```

---

## 二进制

C++没有直接：

```cpp
cout<<bin;
```

需要：

```cpp
bitset
```

---

## 八进制

```cpp
cout<<oct<<100;
```

输出：

```
144
```

---

## 十六进制

```cpp
cout<<hex<<100;
```

输出：

```
64
```

---

恢复十进制：

```cpp
cout<<dec;
```

---

# 八、输入带进制数字

---

## 十六进制输入

```cpp
int x;

cin>>hex>>x;
```

输入：

```
FF
```

输出：

```
255
```

---

## 八进制输入

```cpp
cin>>oct>>x;
```

---

## 十进制

```cpp
cin>>dec>>x;
```

---

# 九、常见算法题处理方法

---

# 1. 判断一个数的二进制1数量

方法：

不断：

```
n&1
```

判断最低位。

代码：

```cpp
int count=0;

while(n)
{
    if(n&1)
        count++;

    n>>=1;
}
```

---

# 2. 判断奇偶

二进制最低位：

```
0 偶数
1 奇数
```

代码：

```cpp
if(n&1)
    cout<<"odd";
else
    cout<<"even";
```

---

# 3. 二进制快速乘除

左移：

```cpp
n<<1
```

相当：

[
n\times2
]

右移：

```cpp
n>>1
```

相当：

[
n/2
]

例：

```cpp
5<<1
```

结果：

```
10
```

---

# 十、位运算基础（进制常考）

## 1. 与 &

规则：

```
1&1=1
其他=0
```

用途：

判断某一位。

---

## 2. 或 |

```
0|0=0
其他=1
```

---

## 3. 异或 ^

规则：

相同0，不同1

用途：

交换数字：

```cpp
a^=b;
b^=a;
a^=b;
```

---

## 4. 取反 ~

所有位反转。

---

# 十一、常见考试模板

---

## 十进制转二进制

```cpp
string bin(int n)
{
    string s;

    while(n)
    {
        s+=n%2+'0';
        n/=2;
    }

    reverse(s.begin(),s.end());

    return s;
}
```

---

## 二进制转十进制

```cpp
int binToDec(string s)
{
    int ans=0;

    for(char c:s)
    {
        ans=ans*2+(c-'0');
    }

    return ans;
}
```

---

## 任意进制转换

```cpp
string change(int n,int base)
{
    string s="";

    while(n)
    {
        int x=n%base;

        if(x<10)
            s+=x+'0';
        else
            s+=x-10+'A';

        n/=base;
    }

    reverse(s.begin(),s.end());

    return s;
}
```

---

# 十二、考试重点优先级

⭐⭐⭐⭐⭐ 必会：

```
1. 除基取余法
2. ans=ans*base+x
3. stoi()
4. bitset
5. 十六进制/二进制互转
6. 位运算 & | ^
```

⭐⭐⭐⭐：

```
oct
hex
dec
iomanip
```

⭐⭐⭐：

```
位移运算
bitset函数
```

---

这部分基本覆盖 **C++上机考试、算法题、计算机组成原理、字符串处理题中所有进制转换需求**。

# 第十四章 C++ 字符串处理专题手册（STL + 高频算法模板）

> 适用场景：
>
> - C++ 上机考试速查
> - 字符串模拟题
> - 信息处理题
> - 密码加密/解密
> - 大整数运算
> - 字符统计
> - 字符串匹配
> - STL string 使用

---

# 目录

```
一、字符串基础
    1. char 与 string
    2. string 常用函数

二、字符处理函数 <cctype>
    1. 大小写转换
    2. 判断字符类型

三、字符串遍历与修改

四、字符统计模板

五、字符串与数字转换

六、进制转换

七、凯撒密码/字符偏移

八、字符串排序

九、字符串比较

十、字符串查找

十一、字符串分割

十二、字符串替换

十三、回文判断

十四、字符串反转

十五、去重

十六、频率统计

十七、最长公共前缀

十八、字符串匹配算法

十九、超大整数运算

二十、综合模板
```

---

# 一、C++字符串基础

## 1. char

单个字符：

```cpp
char c='A';
```

本质：

```
字符 → ASCII码
```

例如：

```cpp
'A'=65
'a'=97
'0'=48
```

所以：

```cpp
cout << ('A'+1);
```

输出：

```
66
```

---

# 2. string

头文件：

```cpp
#include <string>
```

定义：

```cpp
string s;

string s="hello";
```

---

## string本质

类似：

```
字符数组 + 自动扩容
```

例如：

```cpp
string s="abc";
```

内部：

```
a b c \0
```

---

# 二、string常用函数大全

---

# 1. size()/length()

获取长度

```cpp
string s="hello";

cout<<s.size();
```

输出：

```
5
```

二者一样：

```cpp
s.size();

s.length();
```

---

# 2. empty()

判断是否为空

```cpp
if(s.empty())
{
    cout<<"空";
}
```

等价：

```cpp
s.size()==0
```

---

# 3. clear()

清空字符串

```cpp
s.clear();
```

---

# 4. push_back()

尾部添加字符

```cpp
string s="abc";

s.push_back('d');

cout<<s;
```

结果：

```
abcd
```

---

# 5. pop_back()

删除最后字符

```cpp
s.pop_back();
```

---

# 6. append()

追加字符串

```cpp
string a="hello";

a.append(" world");
```

结果：

```
hello world
```

等价：

```cpp
a+=" world";
```

---

# 7. insert()

插入

格式：

```cpp
s.insert(位置,字符串);
```

例：

```cpp
string s="abcd";

s.insert(2,"XX");

cout<<s;
```

结果：

```
abXXcd
```

---

# 8. erase()

删除

## 删除一个位置

```cpp
s.erase(pos);
```

例如：

```cpp
string s="abcdef";

s.erase(2);
```

结果：

```
ab
```

---

## 删除区间

格式：

```cpp
s.erase(pos,len);
```

例：

```cpp
string s="abcdef";

s.erase(2,3);
```

删除：

```
cde
```

结果：

```
abf
```

---

# 9. substr()

截取子串

格式：

```cpp
substr(start,length)
```

例：

```cpp
string s="abcdef";


cout<<s.substr(2,3);
```

输出：

```
cde
```

---

如果省略长度：

```cpp
s.substr(2);
```

输出：

```
cdef
```

---

# 10. find()

查找

格式：

```cpp
s.find(目标);
```

例：

```cpp
string s="hello";


int pos=s.find("ll");


cout<<pos;
```

输出：

```
2
```

找不到：

```cpp
string::npos
```

模板：

```cpp
if(s.find("abc")!=string::npos)
{
    cout<<"存在";
}
```

---

# 11. replace()

替换

格式：

```cpp
replace(pos,len,str)
```

例：

```cpp
string s="abcdef";


s.replace(2,3,"XYZ");


cout<<s;
```

结果：

```
abXYZf
```

---

# 三、字符处理库

头文件：

```cpp
#include<cctype>
```

---

# 1. 判断大写

```cpp
isupper(c)
```

例：

```cpp
if(isupper('A'))
{
}
```

---

# 2. 判断小写

```cpp
islower(c)
```

---

# 3. 判断数字

```cpp
isdigit(c)
```

例：

```cpp
if(isdigit('8'))
{
}
```

---

# 4. 判断字母

```cpp
isalpha(c)
```

---

# 5. 判断字母或数字

```cpp
isalnum(c)
```

---

# 6. 转大写

```cpp
toupper(c)
```

例：

```cpp
char c='a';

c=toupper(c);

cout<<c;
```

输出：

```
A
```

---

# 7. 转小写

```cpp
tolower(c)
```

例：

```cpp
char c='A';

c=tolower(c);
```

结果：

```
a
```

---

# 四、大写转小写模板

## 方法1：tolower

```cpp
for(char &c:s)
{
    c=tolower(c);
}
```

完整：

```cpp
#include<iostream>
#include<string>
using namespace std;


int main()
{
    string s;

    cin>>s;


    for(char &c:s)
    {
        c=tolower(c);
    }


    cout<<s;
}
```

---

# 五、小写转大写

```cpp
for(char &c:s)
{
    c=toupper(c);
}
```

---

# 六、ASCII字符运算

这是字符串题核心。

## 字母距离

例如：

```cpp
'a'-'A'
```

结果：

```
32
```

---

# 大小写转换原理

小写：

```
a b c d
97 98 99 100
```

大写：

```
A B C D
65 66 67 68
```

所以：

小写转大写：

```cpp
c=c-32;
```

大写转小写：

```cpp
c=c+32;
```

但是推荐：

```cpp
toupper()
tolower()
```

---

# 七、凯撒密码模板

## 原理

每个字母移动k位：

例如：

```
ABC

+3

DEF
```

---

## 小写凯撒

```cpp
string s;

cin>>s;


int k;

cin>>k;


for(char &c:s)
{
    c=(c-'a'+k)%26+'a';
}


cout<<s;
```

---

## 防止越界

例如：

```
z+3
```

需要：

```
c-a+k
```

取模：

```
%26
```

---

# 八、字符统计

经典题：

输入：

```
hello
```

输出：

```
h 1
e 1
l 2
o 1
```

---

## 方法1：数组统计

ASCII：

```cpp
int cnt[256]={0};


for(char c:s)
{
    cnt[c]++;
}
```

查询：

```cpp
cout<<cnt['a'];
```

---

# 九、统计26个字母

```cpp
int cnt[26]={0};


for(char c:s)
{
    cnt[c-'a']++;
}
```

例如：

```
a对应0
b对应1
```

---

完整模板：

```cpp
string s;

cin>>s;


int cnt[26]={0};


for(char c:s)
{
    cnt[c-'a']++;
}


for(int i=0;i<26;i++)
{
    if(cnt[i])
    {
        cout<<char(i+'a')
            <<" "
            <<cnt[i]<<endl;
    }
}
```

---

# 十、删除数字字符

例：

输入：

```
a1b2c3
```

输出：

```
abc
```

代码：

```cpp
string s;


cin>>s;


string ans;


for(char c:s)
{
    if(!isdigit(c))
    {
        ans+=c;
    }
}


cout<<ans;
```

---

# 十一、删除空格

```cpp
string ans;


for(char c:s)
{
    if(c!=' ')
        ans+=c;
}
```

---

# 十二、字符串反转

## 方法1

```cpp
reverse(s.begin(),s.end());
```

头文件：

```cpp
#include<algorithm>
```

---

## 方法2

```cpp
string t;


for(int i=s.size()-1;i>=0;i--)
{
    t+=s[i];
}
```

---

# 十三、回文判断

例如：

```
level
```

正反一样。

模板：

```cpp
bool check(string s)
{
    string t=s;

    reverse(t.begin(),t.end());

    return s==t;
}
```

---

# 十四、字符串排序

## 字典序

```cpp
sort(s.begin(),s.end());
```

例：

输入：

```
dcba
```

输出：

```
abcd
```

---

# 十五、去重

## 相邻去重

```cpp
s.erase(unique(s.begin(),s.end()),s.end());
```

例如：

```
aaabbbccc
```

结果：

```
abc
```

注意：

unique前必须排序：

```cpp
sort(s.begin(),s.end());
```

---

# 十六、字符串数字转换

头文件：

```cpp
#include<string>
```

---

# 1. 字符串转整数

```cpp
stoi()
```

例：

```cpp
string s="123";


int x=stoi(s);
```

---

# 2. 转long long

```cpp
stoll()
```

---

# 3. 转double

```cpp
stod()
```

---

# 4. 数字转字符串

```cpp
to_string()
```

例：

```cpp
int x=123;


string s=to_string(x);
```

---

# 十七、stringstream

头文件：

```cpp
#include<sstream>
```

作用：

字符串分割、类型转换。

---

## 字符串转数字

```cpp
stringstream ss("123");


int x;

ss>>x;
```

---

## 多个数字解析

输入：

```
12 34 56
```

代码：

```cpp
string s="12 34 56";


stringstream ss(s);


int a,b,c;


ss>>a>>b>>c;
```

---

# 十八、字符串分割

例如：

```
abc,def,ghi
```

---

模板：

```cpp
vector<string> split(string s,char c)
{
    vector<string> res;

    string temp;


    for(char x:s)
    {
        if(x==c)
        {
            res.push_back(temp);
            temp="";
        }
        else
        {
            temp+=x;
        }
    }


    res.push_back(temp);


    return res;
}
```

---

# 十九、超大整数运算（重点）

## 为什么需要？

long long：

```
最大约 9×10^18
```

但是：

```
100000000000000000000000000000
```

超过范围。

怎么办？

使用：

```
字符串模拟
```

---

# 1. 超大整数加法

思想：

从最低位开始。

例如：

```
123
456

个位相加
十位相加
百位相加
```

模板：

```cpp
string add(string a,string b)
{
    string ans;


    int i=a.size()-1;
    int j=b.size()-1;


    int carry=0;


    while(i>=0||j>=0||carry)
    {
        int sum=carry;


        if(i>=0)
            sum+=a[i--]-'0';


        if(j>=0)
            sum+=b[j--]-'0';


        ans+=char(sum%10+'0');


        carry=sum/10;
    }


    reverse(ans.begin(),ans.end());


    return ans;
}
```

---

# 二十、超大整数乘法

思想：

小学乘法。

例如：

```
123*45
```

模板：

```cpp
string multiply(string a,string b)
{
    vector<int> res(a.size()+b.size());


    for(int i=a.size()-1;i>=0;i--)
    {
        for(int j=b.size()-1;j>=0;j--)
        {
            int x=a[i]-'0';
            int y=b[j]-'0';


            res[i+j+1]+=x*y;
        }
    }


    for(int i=res.size()-1;i>0;i--)
    {
        res[i-1]+=res[i]/10;
        res[i]%=10;
    }


    string ans;


    for(int x:res)
    {
        if(!(ans.empty()&&x==0))
            ans+=x+'0';
    }


    return ans.empty()?"0":ans;
}
```

---

# 二十一、字符串比较数字大小

不能：

```cpp
a>b
```

因为：

```
"99"
>
"100"
```

错误。

方法：

## 比长度

```cpp
bool bigger(string a,string b)
{
    if(a.size()!=b.size())
        return a.size()>b.size();


    return a>b;
}
```

---

# 二十二、常见比赛模板合集

---

## 模板1：统计字符

```cpp
int cnt[256]={0};


for(char c:s)
{
    cnt[c]++;
}
```

---

## 模板2：大小写转换

```cpp
for(char &c:s)
{
    c=tolower(c);
}
```

---

## 模板3：反转

```cpp
reverse(s.begin(),s.end());
```

---

## 模板4：排序

```cpp
sort(s.begin(),s.end());
```

---

## 模板5：查找

```cpp
if(s.find("abc")!=string::npos)
{

}
```

---

## 模板6：去空格

```cpp
s.erase(remove(s.begin(),s.end(),' '),s.end());
```

---

## 模板7：数字字符转整数

```cpp
int x=c-'0';
```

---

## 模板8：整数转字符

```cpp
char c=x+'0';
```

---

# 字符串题考试思维总结

| 题型         | 核心            |
| ------------ | --------------- |
| 大小写转换   | tolower/toupper |
| 字符统计     | cnt数组         |
| 频率最高字符 | 统计+遍历       |
| 密码加密     | ASCII运算       |
| 字符串排序   | sort            |
| 回文         | reverse         |
| 去重         | sort+unique     |
| 查找子串     | find            |
| 截取         | substr          |
| 替换         | replace         |
| 分割         | stringstream    |
| 大整数       | 字符串模拟      |
| 数字转换     | stoi/to_string  |

---

这部分基本覆盖 **C++ 上机考试中 90% 的字符串处理题型**。  
如果作为考场速查手册，建议重点打印：

1. `string函数表`
2. `<cctype>`字符函数
3. 字符统计模板
4. 凯撒密码模板
5. `stringstream`
6. 超大整数加法/乘法模板

这些出现频率最高。

# C++ 常用变量类型数值范围、精度总表（速查版）

> 默认环境：
>
> - `char`：1 Byte
> - `short`：2 Byte
> - `int`：4 Byte
> - `long long`：8 Byte
> - `float`：IEEE 754 单精度
> - `double`：IEEE 754 双精度

| 类型                  |      大小 |          位数 | 有符号范围                                           | 无符号范围               | 最大值（具体数字）       | 大致范围（科学计数法） | 有效精度            |
| --------------------- | --------: | ------------: | ---------------------------------------------------- | ------------------------ | ------------------------ | ---------------------- | ------------------- |
| `bool`                |    1 Byte |             - | `false / true`                                       | -                        | 1                        | -                      | 只有0/1             |
| `char`                |    1 Byte |         8 bit | -128 ~ 127                                           | 0 ~ 255                  | 127 / 255                | 10²                    | 约3位十进制         |
| `signed char`         |    1 Byte |         8 bit | -128 ~ 127                                           | -                        | 127                      | 10²                    | 约3位               |
| `unsigned char`       |    1 Byte |         8 bit | -                                                    | 0 ~ 255                  | 255                      | 10²                    | 约3位               |
| `short`               |    2 Byte |        16 bit | -32768 ~ 32767                                       | 0 ~ 65535                | 32767 / 65535            | 10⁴                    | 约5位               |
| `unsigned short`      |    2 Byte |        16 bit | -                                                    | 0 ~ 65535                | 65535                    | 10⁴                    | 约5位               |
| `int`                 |    4 Byte |        32 bit | -2147483648 ~ 2147483647                             | 0 ~ 4294967295           | 2147483647               | ±2.147 × 10⁹           | 约10位              |
| `unsigned int`        |    4 Byte |        32 bit | -                                                    | 0 ~ 4294967295           | 4294967295               | 4.294 × 10⁹            | 约10位              |
| `long`（Windows）     |    4 Byte |        32 bit | -2147483648 ~ 2147483647                             | 0 ~ 4294967295           | 2147483647               | ±2.147 × 10⁹           | 约10位              |
| `long`（Linux/macOS） |    8 Byte |        64 bit | -9223372036854775808 ~ 9223372036854775807           | 0 ~ 18446744073709551615 | 9223372036854775807      | ±9.223 × 10¹⁸          | 约19位              |
| `long long`           |    8 Byte |        64 bit | -9223372036854775808 ~ 9223372036854775807           | 0 ~ 18446744073709551615 | 9223372036854775807      | ±9.223 × 10¹⁸          | 约19位              |
| `unsigned long long`  |    8 Byte |        64 bit | -                                                    | 0 ~ 18446744073709551615 | 18446744073709551615     | 1.844 × 10¹⁹           | 约20位              |
| `float`               |    4 Byte |        32 bit | -3.4028235×10³⁸ ~ 3.4028235×10³⁸                     | -                        | 3.4028235×10³⁸           | ±3.4 × 10³⁸            | **7位有效数字**     |
| `double`              |    8 Byte |        64 bit | -1.7976931348623157×10³⁰⁸ ~ 1.7976931348623157×10³⁰⁸ | -                        | 1.7976931348623157×10³⁰⁸ | ±1.8 × 10³⁰⁸           | **15~16位有效数字** |
| `long double`         | 8/16 Byte | 64/80/128 bit | 平台相关                                             | -                        | 约10³⁰⁸ ~ 10⁴⁹³²         | 10³⁰⁸~10⁴⁹³²           | 18~19位甚至更高     |

---

# C++ 最重要类型极限速查表

| 类型                 |               最小值 |               最大值 |      约等于 |
| -------------------- | -------------------: | -------------------: | ----------: |
| `int`                |          -2147483648 |           2147483647 |  ±2.1 × 10⁹ |
| `unsigned int`       |                    0 |           4294967295 |   4.3 × 10⁹ |
| `long long`          | -9223372036854775808 |  9223372036854775807 | ±9.2 × 10¹⁸ |
| `unsigned long long` |                    0 | 18446744073709551615 |  1.8 × 10¹⁹ |
| `float`              |            -3.4×10³⁸ |             3.4×10³⁸ |        10³⁸ |
| `double`             |           -1.8×10³⁰⁸ |            1.8×10³⁰⁸ |       10³⁰⁸ |

---

# 浮点类型精度重点表

| 类型          | 存储结构            | 指数范围        | 尾数位     | 十进制有效数字 |
| ------------- | ------------------- | --------------- | ---------- | -------------- |
| `float`       | 1符号+8指数+23尾数  | 10⁻³⁸ ~ 10³⁸    | 23 bit     | **7位**        |
| `double`      | 1符号+11指数+52尾数 | 10⁻³⁰⁸ ~ 10³⁰⁸  | 52 bit     | **15~16位**    |
| `long double` | 平台相关            | 10⁻³⁰⁸ ~ 10⁴⁹³² | 64 bit以上 | **18~19位**    |

---

# 常用选择建议表

| 场景             | 推荐类型                 | 原因           |
| ---------------- | ------------------------ | -------------- |
| 普通整数计算     | `int`                    | 速度快，范围够 |
| 大范围整数       | `long long`              | 最大约9×10¹⁸   |
| 数组下标         | `int` / `size_t`         | STL常用        |
| 钱、概率、平均值 | `double`                 | 精度足够       |
| 图形、游戏计算   | `float`                  | 节省内存       |
| 科学计算         | `double` / `long double` | 精度高         |
| 超过9×10¹⁸整数   | 字符串高精度             | 基础类型不够   |

---

# C++头文件查询极限值

```cpp
#include <climits>
#include <cfloat>
```

| 常量        | 含义              |
| ----------- | ----------------- |
| `INT_MAX`   | int最大值         |
| `INT_MIN`   | int最小值         |
| `LLONG_MAX` | long long最大值   |
| `LLONG_MIN` | long long最小值   |
| `FLT_MAX`   | float最大值       |
| `DBL_MAX`   | double最大值      |
| `LDBL_MAX`  | long double最大值 |

---

# 考试最应该背的 6 个数字

| 类型                     | 必须记                            |
| ------------------------ | --------------------------------- |
| `int`最大                | `2147483647 ≈ 2.1×10⁹`            |
| `long long`最大          | `9223372036854775807 ≈ 9.2×10¹⁸`  |
| `unsigned long long`最大 | `18446744073709551615 ≈ 1.8×10¹⁹` |
| `float`范围              | `3.4×10³⁸`                        |
| `float`精度              | 7位                               |
| `double`精度             | 15~16位                           |
| `double`范围             | `1.8×10³⁰⁸`                       |

---

## 一句话记忆：

```
int       → 10^9级
long long → 10^18级
float     → 10^38范围，7位精度
double    → 10^308范围，15~16位精度
```

这张表基本可以直接放进你的 C++ 考试速查手册。
